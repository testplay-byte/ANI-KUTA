# Project Overview — ANI-KUTA

> What this project is. (To be expanded as we learn more from the user.)

## Summary
ANI-KUTA is an Android application, rebuilt from scratch from a previous working version.

## Goals
1. **Well-documented** — every module, decision, and change recorded.
2. **Modular** — logic split into independent modules.
3. **Highly customizable UI** — frontend independent of backend.
4. **Future-proof** — clean architecture, easy to extend.
5. **Manageable** — a companion web dashboard visualizes the project.

## What We Know So Far
- Old version works but was not planned/documented properly.
- New version is a clean rebuild in a fresh GitHub repo.
- APKs built only via GitHub Actions, ARM64-v8a + armeabi-v7a only.

## What We Don't Know Yet (→ open questions)
- ❓ What does the app actually do? (purpose, main features)
- ❓ Where is the old project? (repo link or local path, for reference)
- ❓ Exact tech stack (Kotlin + Compose assumed).
- ❓ App ID / package name.
- ❓ Target Android versions.
- ❓ Backend data sources (API? local DB? both?).

## Scope
- Android app (primary).
- Companion web dashboard (Next.js, for visualization — secondary).
