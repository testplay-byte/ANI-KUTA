# Lessons Learned — Self-Learning Log

> One-line lessons from mistakes, corrections, and insights.
> Read at task start (grep for tags matching your task type). Append when you learn something.

## How to Use

- **Trigger**: log an entry when (a) the user corrects you, OR (b) you catch your own mistake before/after continuing, OR (c) you have an insight worth remembering.
- **Format**: `- [TAG] lesson (source: <task-id or "self">, <YYYY-MM-DD>)`
- **Tags**: `MISTAKE` (you did wrong), `CORRECTION` (user fixed you), `INSIGHT` (realized), `PATTERN` (recurring).
- **Dedup**: grep existing entries for the keyword before adding. Don't log the same lesson twice.
- **Stale**: mark `~~strikethrough~~` with `→ superseded by <ref>` when a newer entry contradicts.
- **Promote**: if a lesson is a recurring `PATTERN`, also add a one-line rule to `CORE_RULES.md`.

---

## Entries

- [MISTAKE] Don't reference `@android:color/system_accent1_500` (API 31+) when minSdk is 24 — AAPT2 fails the build. Use a static color + `values-v31/` override. (source: Task 7, 2026-08-01)
- [MISTAKE] The Gradle `-PabiFilters=...` flag does nothing unless `build.gradle.kts` reads it. ABI config lives in `ndk.abiFilters` only. (source: Task 9, 2026-08-01)
- [PATTERN] Sub-agent plan reviews catch real flaws — always run one before big changes, and verify findings before acting. (source: Tasks 7, 9, 2026-08-01)
- [MISTAKE] GitHub tokens embedded in git remote URLs leak via `.git/config`. Use credential store instead. (source: Task 10, 2026-08-01)
- [INSIGHT] When consolidating docs, grep for stale cross-references that point to removed files — they break silently. (source: Task 3 review, 2026-08-01)
- [MISTAKE] `kotlinOptions { }` inside `android { }` is deprecated in Kotlin 2.0+. Use top-level `kotlin { compilerOptions { } }`. (source: Task 7, 2026-08-01)
- [INSIGHT] When the user says "I provided X file" but it's not in the upload folder, STOP and flag it — never fabricate the missing content. Do the independent work; pause the dependent work. (source: self, 2026-08-01, design.md missing)
- [MISTAKE] Don't mix old-project analysis docs with new-project architecture docs. `REFERENCES/old-kuta/DOCUMENTATION/` is for OLD project analysis only. New project research/plans go in `APP/ani-kuta/DOCUMENTATION/`. Always check the documentation zone (CORE_RULES.md §21) before writing a doc. (source: self, 2026-08-02, docs 10-16 placed in wrong folder)
- [PATTERN] Don't blindly trust sub-agent output — verify findings by reading the source files yourself before acting. Sub-agents find real flaws, but some may be false positives. (source: user feedback, 2026-08-02)
- [MISTAKE] Build-logic convention plugins need Maven artifact coordinates (e.g. `com.android.tools.build:gradle:8.9.1`), NOT plugin IDs (e.g. `com.android.application:8.9.1`). Use `libs.versions.X.get()` to reference the version. (source: Phase 2 CI iteration 1, 2026-08-02)
- [MISTAKE] Nav3 1.1.5 requires compileSdk 36, not 35. Always check if new deps require a higher SDK. (source: Phase 2 CI iteration 2, 2026-08-02)
- [MISTAKE] SQLDelight 2.3.2 has compatibility issues with Kotlin 2.2.0 — use 2.0.2 (the old project's proven version). Don't always use the "latest" without verifying compatibility. (source: Phase 2 CI iteration 2, 2026-08-02)
- [PATTERN] When a module exposes a third-party type in its public API (return type, constructor parameter), use `api(libs.X)` not `implementation(libs.X)`. Otherwise consumers get "Cannot access class" errors. (source: Phase 2 CI iteration 4, 2026-08-02)
- [INSIGHT] CI `timeout-minutes` includes queue wait time. Set it to 30+ min for GitHub-hosted runners (queue delays of 10-15 min are common). (source: Phase 2 CI iteration 3, 2026-08-02)
- [MISTAKE] `count` is a reserved keyword in SQLDelight's SQL grammar — can't use as a column alias. Use `event_count` or another non-reserved name. (source: Phase 3a CI iteration 1, 2026-08-02)
- [MISTAKE] Kotlin property + function with same name cause JVM signature clash. A `val trustedFingerprints` generates `getTrustedFingerprints()`, which clashes with `fun getTrustedFingerprints()`. Rename the function (e.g., `getAllTrusted()`). (source: Phase 3b CI iteration 3, 2026-08-02)
- [MISTAKE] SQLDelight generates query property names from .sq filename — does NOT convert snake_case to camelCase. Use camelCase filenames (e.g., `downloadQueue.sq` not `download_queue.sq`). (source: Phase 3c CI iteration 3, 2026-08-02)
- [MISTAKE] `AnimeHttpSource.fetchVideoList` returns `Observable<List<Video>>` (RxJava), not `List<Video>`. Use `.awaitSingle()` to convert to suspend. (source: Phase 3c CI iteration 2, 2026-08-02)
- [MISTAKE] `MPVLib.observeProperty` takes Int format constants (`MPVLib.mpvFormat.MPV_FORMAT_INT64`), not String labels. (source: Phase 3c CI iteration 2, 2026-08-02)
- [MISTAKE] When a module uses Flow/StateFlow in its public API (interface return types, abstract class fields), use `api(libs.kotlinx.coroutines.core)` not `implementation`. Otherwise consumers can't resolve the types. (source: Phase 3d CI iteration 2, 2026-08-02)
- [MISTAKE] Watch for naming typos — `TrackLoginState` vs `TrackerLoginState` (missing "er"). Always verify class names match exactly. (source: Phase 3d CI iteration 3, 2026-08-02)
- [MISTAKE] ContentTransform's third parameter (sizeTransform) expects Float, not Int. Use `1f` not `1`. (source: Phase 4a CI iteration 2, 2026-08-02)
- [MISTAKE] `Modifier.heightIn(max = X)` on an INNER scrollable (LazyColumn/verticalScroll) inside a ModalBottomSheet does NOT cap the whole sheet — the sheet grows to inner(70%) + header + tabs ≈ 85%. Apply `heightIn(max)` to the sheet's ROOT Column so the inner scrollable is constrained by the parent (wraps when short, scrolls when tall). (source: user feedback, 2026-08-03, library CustomizeSheet exceeded limit)
- [CORRECTION] Static placeholder UI disappoints users even when documented as "ponytail/Phase 5". If a feature (palettes, color pickers) is shown in the UI, wire it to actually do SOMETHING — a no-op `onClick` that the user can see does nothing erodes trust. Either make it functional or clearly mark it disabled. (source: user feedback, 2026-08-03, "none of the palettes get applied")
- [INSIGHT] Derive a full accent color family (primary + container + onContainer, light+dark) from a single seed via `lerp(seed, surface/text, fraction)` instead of hand-tuning 80 color values per preset. Consistent, maintainable, and 10 presets cost ~10 seed colors. (source: D-053 accent system, 2026-08-03)
- [PATTERN] Keep documentation (progress.md, changelog, decisions) updated IN THE SESSION the work happens — not "later". Stale docs (progress.md said "Phase 2" when we were in Phase 4) mislead the next session and break user trust. CORE_RULES §6 + §25 exist for this reason. (source: user feedback, 2026-08-03, "properly follow the core rules, like proper documentation")
- [INSIGHT] Speech-to-text: "phase 5-2" likely means "phase 5 too" (homophone). Interpret relative-phase phrasing from context before assuming a sub-phase numbering scheme. (source: user message, 2026-08-03, CORE_RULES §13)
- [MISTAKE] Don't plan identity-system-first when the user wants a functional app. The watch flow only needs a minimal source link (one DB row), NOT the full ContentUID graph. Putting identity first blocks ALL testing until the entire graph is built — wrong priority. Plan order: functional first (extensions → details → watch), refinements second (identity → history → backup). (source: user feedback, 2026-08-03, D-054 — first Phase 5 plan rejected)
- [PATTERN] Doc-drift is silent and corrosive. progress.md said "Phase 2" while the project was in Phase 4; the dashboard sidebar still showed "Phase 3" after Phase 4 work. Both user-caught. Root cause: no verification gate at task end. → promoted to CORE_RULES §26 (Documentation Verification — Continuous). Always grep for stale phase/module/decision refs before declaring a task done; audit dashboard NAV_ITEMS for completed-phase labels. (source: user feedback, 2026-08-03)
- [MISTAKE] When updating the dashboard, don't just update data files — audit the NAV_ITEMS array too. A "Phase 3" sidebar link left behind after Phase 4 is done makes the whole dashboard feel stale even if the data is current. The nav must reflect CURRENT state, not history. (source: user feedback, 2026-08-03, "still shows me Phase 3 in the sidebar")
- [CORRECTION] The dark-mode toggle in a scrolling page Header feels broken to the user even when `sticky top-0` is set. Preference: put the toggle in the stationary Sidebar (already sticky), remove the page-level Header. "Stationary" means it never moves regardless of scroll — the sidebar satisfies this, a sticky header can still feel like it scrolls on some viewports. (source: user feedback, 2026-08-03)
- [MISTAKE] The extension loader used the WRONG metadata keys (`ani.source.class`) instead of the Aniyomi convention (`tachiyomi.animeextension.class` + the `tachiyomi.animeextension` feature flag). Result: NO real Aniyomi extensions were discovered — installed extensions were invisible, the "Untrusted" section was always empty. Root cause: I invented custom keys instead of checking the old project's constants. ALWAYS verify metadata keys / contract conventions against the reference (old project or Aniyomi) when porting a binary-compat layer. D-027 is explicit about keeping Aniyomi compat. (source: user feedback, 2026-08-03, "there was no untrusted section, due to which I could not see any of the downloaded extensions")
- [PATTERN] When a feature shows a list of installable items (extensions, downloads, etc.), filter out the ones already installed. Showing an "Install" button on an already-installed item erodes trust. Track install state per-item and show a spinner during install + hide/remove on completion. (source: user feedback, 2026-08-03, "I was even shown the extensions which were already downloaded")
- [INSIGHT] ChildFirstPathClassLoader is REQUIRED for Aniyomi extension loading — the extension's bundled deps (Jsoup, OkHttp) must win over the app's classpath, with binary-compat only at the source-api boundary. A plain PathClassLoader causes NoClassDefFoundError when the extension's dep version differs from the app's. (source: loader fix, 2026-08-03)
- [MISTAKE] `kotlinx.coroutines.sync.withLock` is a suspend function — it can only be called inside a coroutine body, NOT inside a `flow {}` builder's lambda (which is a non-suspend context). Either use `flow { ... }.flowOn(Dispatchers.IO)` with a `mutex.withLock` inside a `launch`, or restructure so the lock is inside a suspend scope. (source: CI failure, 2026-08-03)
- [MISTAKE] `intent.resolveActivity()` returns null on Android 11+ (API 30+) for implicit intents like `ACTION_DELETE` due to package-visibility filtering — EVEN THOUGH `startActivity()` would succeed. Do NOT guard with `resolveActivity()`; just `startActivity()` in try/catch. Add a `<queries>` block in the manifest for defensive visibility. (source: user feedback, 2026-08-03, uninstall dialog not showing)
- [MISTAKE] `untrustExtension` must actually REVOKE the trust by fingerprint (`trustService.revoke(signatureHash)`) — just calling `loadAll()` re-trusts the same fingerprint because it's still in the trusted set. Store `signatureHash` on the `Installed` model so untrust has the fingerprint to revoke. (source: user feedback, 2026-08-03, "some extensions do not get untrusted")
- [INSIGHT] Extension icons: use `appInfo.loadIcon(packageManager)` to get the launcher icon Drawable, then render with Coil's `AsyncImage(model = drawable)`. Coil accepts Drawable as a model — no manual Drawable→ImageBitmap conversion needed. (source: icon fix, 2026-08-03)
- [PATTERN] When a screen has BOTH a verticalScroll path AND a LazyGrid path (e.g. Search with Idle vs Success states), the `collapsed` computation must check BOTH scroll states. Checking only `scrollState.value` misses grid scrolling → the header never collapses when browsing results. (source: user feedback, 2026-08-03, search top section doesn't hide)
- [PATTERN] When a tool (Bash, Read, etc.) fails repeatedly, STOP after 5 tries — don't hammer it. The environment often self-recovers if you pause. → promoted to CORE_RULES §27 (Tool Failure Recovery). (source: Bash failures, 2026-08-03, user directive)
- [MISTAKE] Aniyomi extensions use Injekt (a service locator) to resolve NetworkHelper, Application, Context, and Json. `AnimeHttpSource.network` is `by injectLazy()` — the first HTTP request triggers `Injekt.get<NetworkHelper>()`. Without Injekt registration in App.onCreate(), extensions crash with "No registered instance on Factory for type class ...NetworkHelper". The source-api module had Injekt as a dep + AnimeHttpSource used injectLazy + NetworkHelper was a class — but the REGISTRATION was missing. Always register Injekt singletons in App.onCreate() BEFORE any extension loads. (source: user feedback, 2026-08-03, "No registered instance on Factory for type class eu.kanade.tachiyomi.network.NetworkHelper")
- [MISTAKE] Compose DropdownMenu must be wrapped in a Box with its anchor button — NOT overlaid at a fixed position (Alignment.TopEnd). Overlaid menus appear at the wrong position (bottom-left). The Box wrapper makes the menu appear anchored to the button. (source: user feedback, 2026-08-03, "menu showed on the bottom left corner instead of showing with it")
- [MISTAKE] RxJava `Observable.awaitSingle()` does NOT automatically move network IO to a background thread. The Observable's `subscribeOn`/`observeOn` determine the thread, and Aniyomi extensions often don't set these. ALWAYS wrap `fetchVideoList().awaitSingle()` / `fetchEpisodeList().awaitSingle()` / `fetchSearchAnime().awaitSingle()` in `withContext(Dispatchers.IO)` to avoid `NetworkOnMainThreadException`. (source: user feedback, 2026-08-03, "Resolution failed: android.os.NetworkOnMainThreadException")
- [MISTAKE] OkHttp version MUST match what Aniyomi extensions compile against. The app used OkHttp 4.12.0 but extensions expect 5.0.0-alpha.14. The Kotlin metadata hash for `CacheControl.Builder.maxAge()` changed between versions → "No virtual method maxAge-LRDsOJo(J)Lokhttp3/CacheControl$Builder;" at runtime. ALWAYS use the exact OkHttp version the reference project uses when porting a binary-compat layer. (source: user feedback, 2026-08-03, "No virtual method maxAge-LRDsOJo")
- [PATTERN] Error messages should include the exception TYPE (class simple name) + the message, not just the message. "UnknownHostException: Unable to resolve host" is far more debuggable than just "Unable to resolve host". Format: `"${e::class.java.simpleName}: ${e.message}"`. (source: user feedback, 2026-08-03, user couldn't tell what kind of error it was)
- [INSIGHT] Some Aniyomi extensions override `getSearchAnime` and show a message like "Not used. Get search anime is overwritten." This is an extension-side message, not a crash. The extension doesn't support the standard search API — handle gracefully (show the message, don't crash). This is expected behavior for some extensions. (source: user feedback, 2026-08-03)
- [MISTAKE] MPVLib's `EventObserver` and `LogObserver` interfaces use **non-nullable** params (`String`, not `String?`). Overriding with nullable params causes "overrides nothing" compile errors. Always match the exact param types from the AAR: `event(Int)`, `eventProperty(String)`, `eventProperty(String, Long)`, `eventProperty(String, Boolean)`, `eventProperty(String, String)`, `eventProperty(String, Double)`, `efEvent(String?)`, `logMessage(String, Int, String)`. (source: CI failure, 2026-08-03)
- [PATTERN] Cross-module resource lookup: when an `AndroidView` needs to inflate a layout from a different module (e.g. `R.layout.mpv_view` lives in `:core:player` but the `AndroidView` is in `:feature:watch`), use `ctx.resources.getIdentifier("mpv_view", "layout", ctx.packageName)` instead of `R.layout.mpv_view` (which is module-specific and won't resolve cross-module). (source: WatchScreen implementation, 2026-08-03)
- [MISTAKE] `SAnimeImpl.title` is `lateinit` — accessing it without setting it throws `UninitializedPropertyAccessException`. When creating an SAnime for `fetchEpisodeList`, ALWAYS set both `url` AND `title`. The old project's `ExtensionDetailsProvider` sets `url + title + initialized=false`. Creating `SAnime.create().apply { url = animeUrl }` (title unset) causes extensions that read `sAnime.title` in `episodeListRequest()` to crash with "Failed to load episodes". (source: user feedback + research, 2026-08-03)
- [MISTAKE] When calling `fetchVideoList(episode)`, pass the FULL `SEpisode` from the episode list — NOT a minimal `SEpisode.create().apply { url = ...; name = "Episode" }`. Extensions may read `episode.episode_number` to construct API URLs (e.g. `/episode/${episode_number}`). The default `episode_number = -1f` produces `/episode/-1` → 404 → "Resolution failed". (source: user feedback + research, 2026-08-03)
- [MISTAKE] The old project's `ResolverService` tries `getHosterList(episode)` FIRST (ext-lib 16+ API), then falls back to `getVideoList(episode)`. Extensions that ONLY implement the hoster-based API return empty from `getVideoList` → "No videos". ALWAYS try `getHosterList` first, catch `IllegalStateException` (not supported), then fall back. (source: research, 2026-08-03)
- [INSIGHT] `Hoster` class fields: `hosterName` (NOT `name`), `videoList: List<Video>?` (nullable — check before `isNotEmpty()`). The `getVideoList(hoster)` suspend function resolves a hoster's videos. (source: CI failure, 2026-08-03)
- [MISTAKE] Call `getAnimeDetails(sAnime)` BEFORE `getEpisodeList(sAnime)`. The search result's `sAnime.url` may be a relative URL without a leading "/" (e.g. "mushoku-tensei-..."). The default `episodeListRequest` builds `baseUrl + anime.url` → "https://anikototv.to" + "mushoku-tensei-..." = "https://anikototv.tomushoku-tensei-..." (missing "/" → UnknownHostException). `getAnimeDetails` fetches the anime's page and returns an enriched SAnime with the correct URL format. (source: user logcat analysis, 2026-08-03) ← ~~strikethrough~~ → superseded by the real fix below.
- [MISTAKE] The REAL root cause of "Failed to load episodes" was calling `fetchEpisodeList(sAnime).awaitSingle()` (the Observable version) instead of `getEpisodeList(sAnime)` (the suspend version). Extensions like AniKotoS override `getEpisodeList` (suspend) to use a WebView-based fetch. The DEFAULT `AnimeHttpSource.fetchEpisodeList` builds `baseUrl + anime.url` (missing "/" → UnknownHostException). ALWAYS use the suspend versions: `getEpisodeList`, `getSearchAnime`, `getPopularAnime` — NOT the deprecated Observable `fetch*` versions. The `getAnimeDetails` enrichment was a WRONG fix — the old app doesn't call it before `getEpisodeList`. (source: user logcat comparison old vs new, 2026-08-03)
- [MISTAKE] `BackHandler(enabled = true)` intercepts ALL back presses, including ones triggered by orientation changes. When the user taps fullscreen, the orientation change to landscape can trigger a configuration change that fires the back handler → exits to home. FIX: only enable BackHandler in fullscreen mode: `BackHandler(enabled = playerMode == PlayerMode.FULLSCREEN)`. (source: user feedback, 2026-08-03, "instead of going to full screen mode it directly led me to the home page") ← ~~strikethrough~~ → superseded: the real fix was `android:configChanges` (see below).
- [MISTAKE] When `requestedOrientation` changes (e.g. portrait → landscape for fullscreen), Android RECREATES the Activity by default. Activity recreation destroys all `remember` state (including the Nav3 backstack) → user lands on the home page. FIX: add `android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"` to the Activity in AndroidManifest.xml. This tells Android NOT to recreate — the app handles orientation changes via Compose's adaptive layout. The old project does this too. (source: user logcat, 2026-08-03, "Checking to restart ActivityRecord... changed=0x480")
- [MISTAKE] Compose `LazyColumn`/`LazyRow` `items(list, key = { it.url })` crashes with `IllegalArgumentException: Key "" was already used` when two items have the same empty URL. FIX: use `itemsIndexed(list, key = { index, item -> "$index:${item.url}" })` — guaranteed unique even when fields are empty. (source: user crash logcat, 2026-08-03)
- [MISTAKE] MPV's `mpv.conf` does NOT support shell-style template variables like `${sub-font-size}`. These are treated as literal strings → "Error parsing option sub-font-size: option parameter could not be parsed". ALWAYS use actual values in mpv.conf (e.g. `sub-font-size=55`, `sub-color=#FFFFFFFF`). (source: user logcat, 2026-08-03, "setting option sub-font-size='${sub-font-size}' failed")
- [MISTAKE] When porting Compose UI components to `:core:player`, the module must use `anikuta.library.compose` plugin (NOT `anikuta.library`) and declare `material-icons-extended` as a dependency. Without compose deps, `import androidx.compose.material.icons.*` fails with "Unresolved reference 'material'". (source: CI failure, 2026-08-03)
- [MISTAKE] Empty `initOptions()` → no video. The new project's `AnikutaMPVView.initOptions()` was a no-op stub ("No custom init options needed") — `setVo("gpu")` was never called → MPV had no video output configured (audio played, screen stayed black). Always port the FULL `initOptions()` from the reference project: `setVo`, `profile=fast`, `hwdec=auto`, `keep-open`, `input-default-bindings`, `ytdl=no`, `tls-verify`, `tls-ca-file`, `demuxer-max-bytes=256MB`, `demuxer-max-back-bytes=64MB`, `vd-lavc-film-grain=cpu`, `applySubtitlePreferencesInit()`. An empty `initOptions()` means MPV has no video output configured. (source: RESEARCH-OLD-PLAYER + D-061, 2026-08-03, "audio plays but no video")
- [MISTAKE] `setDecorFitsSystemWindows(true)` conflicts with `enableEdgeToEdge()`. Never call `setDecorFitsSystemWindows(window, true)` in a screen that's part of an edge-to-edge app — it overrides the global `enableEdgeToEdge()` (which sets it to `false`) and persists on the Activity after the screen exits (Compose has no auto-restore). Result: framework auto-pads the content view for the status bar AND Compose `statusBarsPadding()` also applies the inset → DOUBLE PADDING on subsequent screens. The old project NEVER sets it `true` in minimized player mode — it only calls `controller.show(systemBars())` and lets the global `false` value stand. (source: RESEARCH-PADDING-BUG + D-062, 2026-08-03, "extra top padding on Browse after exiting player")
- [MISTAKE] Empty `onDispose { }` leaks window/system-bar state. When a `DisposableEffect` modifies window-level state (`setDecorFitsSystemWindows`, `requestedOrientation`, `WindowInsetsControllerCompat.hide/show`), the `onDispose` block MUST restore the app-wide defaults. An empty `onDispose` leaves the last mode's state on the Activity → next screen sees a corrupted window (wrong insets, wrong orientation, hidden bars). Always clean up window/system-bar changes in `onDispose` when leaving a screen. (source: RESEARCH-PADDING-BUG + D-062, 2026-08-03)
- [MISTAKE] `setPropertyString` doesn't reliably update numeric MPV properties at runtime. For numerics (font size, border size, position, delay, shadow offset, etc.), use `MPVLib.setPropertyInt("sub-font-size", value)` or `MPVLib.setPropertyDouble("sub-delay", value)` — NOT `setPropertyString("sub-font-size", value.toString())`. The string variant may silently fail or not trigger the property-change callback that re-renders subtitles. Use `setPropertyString` only for true string properties (font name, color hex, language). (source: D-064 SubtitleSettingsSheet port, 2026-08-03)
- [MISTAKE] `hwdec=auto-copy` fails on some devices. `auto-copy` forces GPU decode → CPU copy-back (one extra memcpy + may hit a path without a working decoder). The old project uses `hwdec=auto` (zero-copy via SurfaceView + MediaCodec), which is the recommended path for Android SurfaceView rendering. Use `hwdec=auto` in mpv.conf OR via `setOptionString("hwdec", "auto")` in `initOptions()`. (source: RESEARCH-OLD-PLAYER + D-061, 2026-08-03)
- [MISTAKE] Don't clear error state in the `END_FILE` MPV event — `END_FILE` fires normally on quality switch (the old file ends as the new one loads). If you treat `END_FILE` as "error cleared", you'll hide real errors that occurred mid-playback AND you'll race with the next `FILE_LOADED`. Only clear error state on `FILE_LOADED` (the new file successfully started) OR on explicit user action (tap "retry"). The new project's `PlayerObserver` was failing to clear the "Loading failed" overlay because it didn't handle `FILE_LOADED` → the user was stuck on the error overlay even after a successful quality switch. Fix: `onEvent(FILE_LOADED)` calls `updateError(null)` + `updateLoadingState(READY)` + loads tracks. (source: Phase 5c loading-overlay fix, 2026-08-03)
- [CORRECTION] NEVER install the Android SDK, JDK (with javac), or any Android build tooling in the local environment. The user explicitly forbade this. The local environment is for CODE ANALYSIS only — reading files, writing code, using subagents. GitHub Actions provides ALL build tooling. If you need to find a compile error: (1) read the code carefully line by line, (2) use Explore subagents to review, (3) cross-reference against the old project, (4) push to CI and read failure annotations from the GitHub API. NEVER run `./gradlew` locally. NEVER write `sdk.dir=` to `local.properties`. NEVER download `commandlinetools-linux` or `jdk-*-linux-x64_bin.tar.gz`. (source: user correction, 2026-08-03, "never supposed to install Android SDK")
- [MISTAKE] `BorderStroke` is in `androidx.compose.foundation`, NOT `androidx.compose.material3`. Importing from `material3` causes "Unresolved reference 'BorderStroke'" compile error. Always use `import androidx.compose.foundation.BorderStroke`. (source: CI failure 2026-08-04, commit 009dce43)
- [INSIGHT] When CI fails with "Compilation error. See log for more details" and the subagents can't find the error by code review, the ACTUAL error is in the CI build log. Download it via: extract token from `git config --get remote.origin.url`, then `curl -H "Authorization: token $TOKEN" "https://api.github.com/repos/{owner}/{repo}/actions/runs/{id}/logs" -L -o logs.zip`, then `unzip logs.zip && grep "^e:" *.txt`. This is the ONLY reliable way to find the exact error. (source: CI debugging session, 2026-08-04)
- [MISTAKE] When rewriting a Compose file from scratch, ALWAYS check that every Foundation function (`background`, `clickable`, `border`, `clip`, etc.) is imported. The compiler error "Unresolved reference 'X'" usually means a missing `import androidx.compose.foundation.X` statement. Before pushing, grep the file for all function calls and verify each has an import. (source: CI failure 2026-08-04, PlayerSheets.kt missing `background` import)
- [MISTAKE] `Video.url` is DEPRECATED — it returns `videoPageUrl` (the episode page URL), NOT the stream URL. Extensions that use the primary constructor leave `videoPageUrl` empty. ALWAYS use `video.videoUrl` for the actual playable stream URL. The deprecation comment says "Use videoTitle instead" which is misleading — `videoTitle` is the quality label, not the URL. The correct field is `video.videoUrl`. (source: CI debugging session 2026-08-04, log showed `URL: ` empty, root cause of "loading failed")
