# Library Tables (DEFERRED to Phase 4)

> These tables are NOT yet built. They will be added in Phase 4 when the identity system is implemented.

## Planned Tables
- `category` — user-defined categories (Watching, Completed, etc.).
- `library_entry` — anime in the user's library.
- `library_entry_category` — many-to-many between library entries and categories.
