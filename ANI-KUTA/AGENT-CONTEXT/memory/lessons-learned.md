# Lessons Learned — Self-Learning Log

> One-line lessons from mistakes, corrections, and insights.
> Read at task start (grep for tags matching your task type). Append when you learn something.

## How to Use

- **Trigger**: log an entry when (a) the user corrects you, OR (b) you catch your own mistake before/after continuing, OR (c) you have an insight worth remembering.
- **Format**: `- [TAG] lesson (source: <task-id or "self">, <YYYY-MM-DD>)`
- **Tags**: `MISTAKE` (you did wrong), `CORRECTION` (user fixed you), `INSIGHT` (realized), `PATTERN` (recurring).
- **Dedup**: grep existing entries for the keyword before adding. Don't log the same lesson twice.
- **Stale**: mark `~~strikethrough~~` with `→ superseded by <ref>` when a newer entry contradicts.
- **Promote**: if a lesson is a recurring `PATTERN`, also add a one-line rule to `CORE_RULES.md`.

---

## Entries

- [MISTAKE] Don't reference `@android:color/system_accent1_500` (API 31+) when minSdk is 24 — AAPT2 fails the build. Use a static color + `values-v31/` override. (source: Task 7, 2026-08-01)
- [MISTAKE] The Gradle `-PabiFilters=...` flag does nothing unless `build.gradle.kts` reads it. ABI config lives in `ndk.abiFilters` only. (source: Task 9, 2026-08-01)
- [PATTERN] Sub-agent plan reviews catch real flaws — always run one before big changes, and verify findings before acting. (source: Tasks 7, 9, 2026-08-01)
- [MISTAKE] GitHub tokens embedded in git remote URLs leak via `.git/config`. Use credential store instead. (source: Task 10, 2026-08-01)
- [INSIGHT] When consolidating docs, grep for stale cross-references that point to removed files — they break silently. (source: Task 3 review, 2026-08-01)
- [MISTAKE] `kotlinOptions { }` inside `android { }` is deprecated in Kotlin 2.0+. Use top-level `kotlin { compilerOptions { } }`. (source: Task 7, 2026-08-01)
- [INSIGHT] When the user says "I provided X file" but it's not in the upload folder, STOP and flag it — never fabricate the missing content. Do the independent work; pause the dependent work. (source: self, 2026-08-01, design.md missing)
- [MISTAKE] Don't mix old-project analysis docs with new-project architecture docs. `REFERENCES/old-kuta/DOCUMENTATION/` is for OLD project analysis only. New project research/plans go in `APP/ani-kuta/DOCUMENTATION/`. Always check the documentation zone (CORE_RULES.md §21) before writing a doc. (source: self, 2026-08-02, docs 10-16 placed in wrong folder)
- [PATTERN] Don't blindly trust sub-agent output — verify findings by reading the source files yourself before acting. Sub-agents find real flaws, but some may be false positives. (source: user feedback, 2026-08-02)
- [MISTAKE] Build-logic convention plugins need Maven artifact coordinates (e.g. `com.android.tools.build:gradle:8.9.1`), NOT plugin IDs (e.g. `com.android.application:8.9.1`). Use `libs.versions.X.get()` to reference the version. (source: Phase 2 CI iteration 1, 2026-08-02)
- [MISTAKE] Nav3 1.1.5 requires compileSdk 36, not 35. Always check if new deps require a higher SDK. (source: Phase 2 CI iteration 2, 2026-08-02)
- [MISTAKE] SQLDelight 2.3.2 has compatibility issues with Kotlin 2.2.0 — use 2.0.2 (the old project's proven version). Don't always use the "latest" without verifying compatibility. (source: Phase 2 CI iteration 2, 2026-08-02)
- [PATTERN] When a module exposes a third-party type in its public API (return type, constructor parameter), use `api(libs.X)` not `implementation(libs.X)`. Otherwise consumers get "Cannot access class" errors. (source: Phase 2 CI iteration 4, 2026-08-02)
- [INSIGHT] CI `timeout-minutes` includes queue wait time. Set it to 30+ min for GitHub-hosted runners (queue delays of 10-15 min are common). (source: Phase 2 CI iteration 3, 2026-08-02)
- [MISTAKE] `count` is a reserved keyword in SQLDelight's SQL grammar — can't use as a column alias. Use `event_count` or another non-reserved name. (source: Phase 3a CI iteration 1, 2026-08-02)
- [MISTAKE] Kotlin property + function with same name cause JVM signature clash. A `val trustedFingerprints` generates `getTrustedFingerprints()`, which clashes with `fun getTrustedFingerprints()`. Rename the function (e.g., `getAllTrusted()`). (source: Phase 3b CI iteration 3, 2026-08-02)
- [MISTAKE] SQLDelight generates query property names from .sq filename — does NOT convert snake_case to camelCase. Use camelCase filenames (e.g., `downloadQueue.sq` not `download_queue.sq`). (source: Phase 3c CI iteration 3, 2026-08-02)
- [MISTAKE] `AnimeHttpSource.fetchVideoList` returns `Observable<List<Video>>` (RxJava), not `List<Video>`. Use `.awaitSingle()` to convert to suspend. (source: Phase 3c CI iteration 2, 2026-08-02)
- [MISTAKE] `MPVLib.observeProperty` takes Int format constants (`MPVLib.mpvFormat.MPV_FORMAT_INT64`), not String labels. (source: Phase 3c CI iteration 2, 2026-08-02)
- [MISTAKE] When a module uses Flow/StateFlow in its public API (interface return types, abstract class fields), use `api(libs.kotlinx.coroutines.core)` not `implementation`. Otherwise consumers can't resolve the types. (source: Phase 3d CI iteration 2, 2026-08-02)
- [MISTAKE] Watch for naming typos — `TrackLoginState` vs `TrackerLoginState` (missing "er"). Always verify class names match exactly. (source: Phase 3d CI iteration 3, 2026-08-02)
- [MISTAKE] ContentTransform's third parameter (sizeTransform) expects Float, not Int. Use `1f` not `1`. (source: Phase 4a CI iteration 2, 2026-08-02)
- [MISTAKE] `Modifier.heightIn(max = X)` on an INNER scrollable (LazyColumn/verticalScroll) inside a ModalBottomSheet does NOT cap the whole sheet — the sheet grows to inner(70%) + header + tabs ≈ 85%. Apply `heightIn(max)` to the sheet's ROOT Column so the inner scrollable is constrained by the parent (wraps when short, scrolls when tall). (source: user feedback, 2026-08-03, library CustomizeSheet exceeded limit)
- [CORRECTION] Static placeholder UI disappoints users even when documented as "ponytail/Phase 5". If a feature (palettes, color pickers) is shown in the UI, wire it to actually do SOMETHING — a no-op `onClick` that the user can see does nothing erodes trust. Either make it functional or clearly mark it disabled. (source: user feedback, 2026-08-03, "none of the palettes get applied")
- [INSIGHT] Derive a full accent color family (primary + container + onContainer, light+dark) from a single seed via `lerp(seed, surface/text, fraction)` instead of hand-tuning 80 color values per preset. Consistent, maintainable, and 10 presets cost ~10 seed colors. (source: D-053 accent system, 2026-08-03)
- [PATTERN] Keep documentation (progress.md, changelog, decisions) updated IN THE SESSION the work happens — not "later". Stale docs (progress.md said "Phase 2" when we were in Phase 4) mislead the next session and break user trust. CORE_RULES §6 + §25 exist for this reason. (source: user feedback, 2026-08-03, "properly follow the core rules, like proper documentation")
- [INSIGHT] Speech-to-text: "phase 5-2" likely means "phase 5 too" (homophone). Interpret relative-phase phrasing from context before assuming a sub-phase numbering scheme. (source: user message, 2026-08-03, CORE_RULES §13)
- [MISTAKE] Don't plan identity-system-first when the user wants a functional app. The watch flow only needs a minimal source link (one DB row), NOT the full ContentUID graph. Putting identity first blocks ALL testing until the entire graph is built — wrong priority. Plan order: functional first (extensions → details → watch), refinements second (identity → history → backup). (source: user feedback, 2026-08-03, D-054 — first Phase 5 plan rejected)
- [PATTERN] Doc-drift is silent and corrosive. progress.md said "Phase 2" while the project was in Phase 4; the dashboard sidebar still showed "Phase 3" after Phase 4 work. Both user-caught. Root cause: no verification gate at task end. → promoted to CORE_RULES §26 (Documentation Verification — Continuous). Always grep for stale phase/module/decision refs before declaring a task done; audit dashboard NAV_ITEMS for completed-phase labels. (source: user feedback, 2026-08-03)
- [MISTAKE] When updating the dashboard, don't just update data files — audit the NAV_ITEMS array too. A "Phase 3" sidebar link left behind after Phase 4 is done makes the whole dashboard feel stale even if the data is current. The nav must reflect CURRENT state, not history. (source: user feedback, 2026-08-03, "still shows me Phase 3 in the sidebar")
- [CORRECTION] The dark-mode toggle in a scrolling page Header feels broken to the user even when `sticky top-0` is set. Preference: put the toggle in the stationary Sidebar (already sticky), remove the page-level Header. "Stationary" means it never moves regardless of scroll — the sidebar satisfies this, a sticky header can still feel like it scrolls on some viewports. (source: user feedback, 2026-08-03)
- [MISTAKE] The extension loader used the WRONG metadata keys (`ani.source.class`) instead of the Aniyomi convention (`tachiyomi.animeextension.class` + the `tachiyomi.animeextension` feature flag). Result: NO real Aniyomi extensions were discovered — installed extensions were invisible, the "Untrusted" section was always empty. Root cause: I invented custom keys instead of checking the old project's constants. ALWAYS verify metadata keys / contract conventions against the reference (old project or Aniyomi) when porting a binary-compat layer. D-027 is explicit about keeping Aniyomi compat. (source: user feedback, 2026-08-03, "there was no untrusted section, due to which I could not see any of the downloaded extensions")
- [PATTERN] When a feature shows a list of installable items (extensions, downloads, etc.), filter out the ones already installed. Showing an "Install" button on an already-installed item erodes trust. Track install state per-item and show a spinner during install + hide/remove on completion. (source: user feedback, 2026-08-03, "I was even shown the extensions which were already downloaded")
- [INSIGHT] ChildFirstPathClassLoader is REQUIRED for Aniyomi extension loading — the extension's bundled deps (Jsoup, OkHttp) must win over the app's classpath, with binary-compat only at the source-api boundary. A plain PathClassLoader causes NoClassDefFoundError when the extension's dep version differs from the app's. (source: loader fix, 2026-08-03)
- [MISTAKE] `kotlinx.coroutines.sync.withLock` is a suspend function — it can only be called inside a coroutine body, NOT inside a `flow {}` builder's lambda (which is a non-suspend context). Either use `flow { ... }.flowOn(Dispatchers.IO)` with a `mutex.withLock` inside a `launch`, or restructure so the lock is inside a suspend scope. (source: CI failure, 2026-08-03)
- [MISTAKE] `intent.resolveActivity()` returns null on Android 11+ (API 30+) for implicit intents like `ACTION_DELETE` due to package-visibility filtering — EVEN THOUGH `startActivity()` would succeed. Do NOT guard with `resolveActivity()`; just `startActivity()` in try/catch. Add a `<queries>` block in the manifest for defensive visibility. (source: user feedback, 2026-08-03, uninstall dialog not showing)
- [MISTAKE] `untrustExtension` must actually REVOKE the trust by fingerprint (`trustService.revoke(signatureHash)`) — just calling `loadAll()` re-trusts the same fingerprint because it's still in the trusted set. Store `signatureHash` on the `Installed` model so untrust has the fingerprint to revoke. (source: user feedback, 2026-08-03, "some extensions do not get untrusted")
- [INSIGHT] Extension icons: use `appInfo.loadIcon(packageManager)` to get the launcher icon Drawable, then render with Coil's `AsyncImage(model = drawable)`. Coil accepts Drawable as a model — no manual Drawable→ImageBitmap conversion needed. (source: icon fix, 2026-08-03)
- [PATTERN] When a screen has BOTH a verticalScroll path AND a LazyGrid path (e.g. Search with Idle vs Success states), the `collapsed` computation must check BOTH scroll states. Checking only `scrollState.value` misses grid scrolling → the header never collapses when browsing results. (source: user feedback, 2026-08-03, search top section doesn't hide)
- [PATTERN] When a tool (Bash, Read, etc.) fails repeatedly, STOP after 5 tries — don't hammer it. The environment often self-recovers if you pause. → promoted to CORE_RULES §27 (Tool Failure Recovery). (source: Bash failures, 2026-08-03, user directive)
- [MISTAKE] Aniyomi extensions use Injekt (a service locator) to resolve NetworkHelper, Application, Context, and Json. `AnimeHttpSource.network` is `by injectLazy()` — the first HTTP request triggers `Injekt.get<NetworkHelper>()`. Without Injekt registration in App.onCreate(), extensions crash with "No registered instance on Factory for type class ...NetworkHelper". The source-api module had Injekt as a dep + AnimeHttpSource used injectLazy + NetworkHelper was a class — but the REGISTRATION was missing. Always register Injekt singletons in App.onCreate() BEFORE any extension loads. (source: user feedback, 2026-08-03, "No registered instance on Factory for type class eu.kanade.tachiyomi.network.NetworkHelper")
- [MISTAKE] Compose DropdownMenu must be wrapped in a Box with its anchor button — NOT overlaid at a fixed position (Alignment.TopEnd). Overlaid menus appear at the wrong position (bottom-left). The Box wrapper makes the menu appear anchored to the button. (source: user feedback, 2026-08-03, "menu showed on the bottom left corner instead of showing with it")
- [MISTAKE] RxJava `Observable.awaitSingle()` does NOT automatically move network IO to a background thread. The Observable's `subscribeOn`/`observeOn` determine the thread, and Aniyomi extensions often don't set these. ALWAYS wrap `fetchVideoList().awaitSingle()` / `fetchEpisodeList().awaitSingle()` / `fetchSearchAnime().awaitSingle()` in `withContext(Dispatchers.IO)` to avoid `NetworkOnMainThreadException`. (source: user feedback, 2026-08-03, "Resolution failed: android.os.NetworkOnMainThreadException")
- [MISTAKE] OkHttp version MUST match what Aniyomi extensions compile against. The app used OkHttp 4.12.0 but extensions expect 5.0.0-alpha.14. The Kotlin metadata hash for `CacheControl.Builder.maxAge()` changed between versions → "No virtual method maxAge-LRDsOJo(J)Lokhttp3/CacheControl$Builder;" at runtime. ALWAYS use the exact OkHttp version the reference project uses when porting a binary-compat layer. (source: user feedback, 2026-08-03, "No virtual method maxAge-LRDsOJo")
- [PATTERN] Error messages should include the exception TYPE (class simple name) + the message, not just the message. "UnknownHostException: Unable to resolve host" is far more debuggable than just "Unable to resolve host". Format: `"${e::class.java.simpleName}: ${e.message}"`. (source: user feedback, 2026-08-03, user couldn't tell what kind of error it was)
- [INSIGHT] Some Aniyomi extensions override `getSearchAnime` and show a message like "Not used. Get search anime is overwritten." This is an extension-side message, not a crash. The extension doesn't support the standard search API — handle gracefully (show the message, don't crash). This is expected behavior for some extensions. (source: user feedback, 2026-08-03)
- [MISTAKE] MPVLib's `EventObserver` and `LogObserver` interfaces use **non-nullable** params (`String`, not `String?`). Overriding with nullable params causes "overrides nothing" compile errors. Always match the exact param types from the AAR: `event(Int)`, `eventProperty(String)`, `eventProperty(String, Long)`, `eventProperty(String, Boolean)`, `eventProperty(String, String)`, `eventProperty(String, Double)`, `efEvent(String?)`, `logMessage(String, Int, String)`. (source: CI failure, 2026-08-03)
- [PATTERN] Cross-module resource lookup: when an `AndroidView` needs to inflate a layout from a different module (e.g. `R.layout.mpv_view` lives in `:core:player` but the `AndroidView` is in `:feature:watch`), use `ctx.resources.getIdentifier("mpv_view", "layout", ctx.packageName)` instead of `R.layout.mpv_view` (which is module-specific and won't resolve cross-module). (source: WatchScreen implementation, 2026-08-03)
- [MISTAKE] `SAnimeImpl.title` is `lateinit` — accessing it without setting it throws `UninitializedPropertyAccessException`. When creating an SAnime for `fetchEpisodeList`, ALWAYS set both `url` AND `title`. The old project's `ExtensionDetailsProvider` sets `url + title + initialized=false`. Creating `SAnime.create().apply { url = animeUrl }` (title unset) causes extensions that read `sAnime.title` in `episodeListRequest()` to crash with "Failed to load episodes". (source: user feedback + research, 2026-08-03)
- [MISTAKE] When calling `fetchVideoList(episode)`, pass the FULL `SEpisode` from the episode list — NOT a minimal `SEpisode.create().apply { url = ...; name = "Episode" }`. Extensions may read `episode.episode_number` to construct API URLs (e.g. `/episode/${episode_number}`). The default `episode_number = -1f` produces `/episode/-1` → 404 → "Resolution failed". (source: user feedback + research, 2026-08-03)
- [MISTAKE] The old project's `ResolverService` tries `getHosterList(episode)` FIRST (ext-lib 16+ API), then falls back to `getVideoList(episode)`. Extensions that ONLY implement the hoster-based API return empty from `getVideoList` → "No videos". ALWAYS try `getHosterList` first, catch `IllegalStateException` (not supported), then fall back. (source: research, 2026-08-03)
- [INSIGHT] `Hoster` class fields: `hosterName` (NOT `name`), `videoList: List<Video>?` (nullable — check before `isNotEmpty()`). The `getVideoList(hoster)` suspend function resolves a hoster's videos. (source: CI failure, 2026-08-03)
- [MISTAKE] Call `getAnimeDetails(sAnime)` BEFORE `getEpisodeList(sAnime)`. The search result's `sAnime.url` may be a relative URL without a leading "/" (e.g. "mushoku-tensei-..."). The default `episodeListRequest` builds `baseUrl + anime.url` → "https://anikototv.to" + "mushoku-tensei-..." = "https://anikototv.tomushoku-tensei-..." (missing "/" → UnknownHostException). `getAnimeDetails` fetches the anime's page and returns an enriched SAnime with the correct URL format. (source: user logcat analysis, 2026-08-03) ← ~~strikethrough~~ → superseded by the real fix below.
- [MISTAKE] The REAL root cause of "Failed to load episodes" was calling `fetchEpisodeList(sAnime).awaitSingle()` (the Observable version) instead of `getEpisodeList(sAnime)` (the suspend version). Extensions like AniKotoS override `getEpisodeList` (suspend) to use a WebView-based fetch. The DEFAULT `AnimeHttpSource.fetchEpisodeList` builds `baseUrl + anime.url` (missing "/" → UnknownHostException). ALWAYS use the suspend versions: `getEpisodeList`, `getSearchAnime`, `getPopularAnime` — NOT the deprecated Observable `fetch*` versions. The `getAnimeDetails` enrichment was a WRONG fix — the old app doesn't call it before `getEpisodeList`. (source: user logcat comparison old vs new, 2026-08-03)
- [MISTAKE] `BackHandler(enabled = true)` intercepts ALL back presses, including ones triggered by orientation changes. When the user taps fullscreen, the orientation change to landscape can trigger a configuration change that fires the back handler → exits to home. FIX: only enable BackHandler in fullscreen mode: `BackHandler(enabled = playerMode == PlayerMode.FULLSCREEN)`. (source: user feedback, 2026-08-03, "instead of going to full screen mode it directly led me to the home page") ← ~~strikethrough~~ → superseded: the real fix was `android:configChanges` (see below).
- [MISTAKE] When `requestedOrientation` changes (e.g. portrait → landscape for fullscreen), Android RECREATES the Activity by default. Activity recreation destroys all `remember` state (including the Nav3 backstack) → user lands on the home page. FIX: add `android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"` to the Activity in AndroidManifest.xml. This tells Android NOT to recreate — the app handles orientation changes via Compose's adaptive layout. The old project does this too. (source: user logcat, 2026-08-03, "Checking to restart ActivityRecord... changed=0x480")
- [MISTAKE] Compose `LazyColumn`/`LazyRow` `items(list, key = { it.url })` crashes with `IllegalArgumentException: Key "" was already used` when two items have the same empty URL. FIX: use `itemsIndexed(list, key = { index, item -> "$index:${item.url}" })` — guaranteed unique even when fields are empty. (source: user crash logcat, 2026-08-03)
- [MISTAKE] MPV's `mpv.conf` does NOT support shell-style template variables like `${sub-font-size}`. These are treated as literal strings → "Error parsing option sub-font-size: option parameter could not be parsed". ALWAYS use actual values in mpv.conf (e.g. `sub-font-size=55`, `sub-color=#FFFFFFFF`). (source: user logcat, 2026-08-03, "setting option sub-font-size='${sub-font-size}' failed")
- [MISTAKE] When porting Compose UI components to `:core:player`, the module must use `anikuta.library.compose` plugin (NOT `anikuta.library`) and declare `material-icons-extended` as a dependency. Without compose deps, `import androidx.compose.material.icons.*` fails with "Unresolved reference 'material'". (source: CI failure, 2026-08-03)
- [MISTAKE] Empty `initOptions()` → no video. The new project's `AnikutaMPVView.initOptions()` was a no-op stub ("No custom init options needed") — `setVo("gpu")` was never called → MPV had no video output configured (audio played, screen stayed black). Always port the FULL `initOptions()` from the reference project: `setVo`, `profile=fast`, `hwdec=auto`, `keep-open`, `input-default-bindings`, `ytdl=no`, `tls-verify`, `tls-ca-file`, `demuxer-max-bytes=256MB`, `demuxer-max-back-bytes=64MB`, `vd-lavc-film-grain=cpu`, `applySubtitlePreferencesInit()`. An empty `initOptions()` means MPV has no video output configured. (source: RESEARCH-OLD-PLAYER + D-061, 2026-08-03, "audio plays but no video")
- [MISTAKE] `setDecorFitsSystemWindows(true)` conflicts with `enableEdgeToEdge()`. Never call `setDecorFitsSystemWindows(window, true)` in a screen that's part of an edge-to-edge app — it overrides the global `enableEdgeToEdge()` (which sets it to `false`) and persists on the Activity after the screen exits (Compose has no auto-restore). Result: framework auto-pads the content view for the status bar AND Compose `statusBarsPadding()` also applies the inset → DOUBLE PADDING on subsequent screens. The old project NEVER sets it `true` in minimized player mode — it only calls `controller.show(systemBars())` and lets the global `false` value stand. (source: RESEARCH-PADDING-BUG + D-062, 2026-08-03, "extra top padding on Browse after exiting player")
- [MISTAKE] Empty `onDispose { }` leaks window/system-bar state. When a `DisposableEffect` modifies window-level state (`setDecorFitsSystemWindows`, `requestedOrientation`, `WindowInsetsControllerCompat.hide/show`), the `onDispose` block MUST restore the app-wide defaults. An empty `onDispose` leaves the last mode's state on the Activity → next screen sees a corrupted window (wrong insets, wrong orientation, hidden bars). Always clean up window/system-bar changes in `onDispose` when leaving a screen. (source: RESEARCH-PADDING-BUG + D-062, 2026-08-03)
- [MISTAKE] `setPropertyString` doesn't reliably update numeric MPV properties at runtime. For numerics (font size, border size, position, delay, shadow offset, etc.), use `MPVLib.setPropertyInt("sub-font-size", value)` or `MPVLib.setPropertyDouble("sub-delay", value)` — NOT `setPropertyString("sub-font-size", value.toString())`. The string variant may silently fail or not trigger the property-change callback that re-renders subtitles. Use `setPropertyString` only for true string properties (font name, color hex, language). (source: D-064 SubtitleSettingsSheet port, 2026-08-03)
- [MISTAKE] `hwdec=auto-copy` fails on some devices. `auto-copy` forces GPU decode → CPU copy-back (one extra memcpy + may hit a path without a working decoder). The old project uses `hwdec=auto` (zero-copy via SurfaceView + MediaCodec), which is the recommended path for Android SurfaceView rendering. Use `hwdec=auto` in mpv.conf OR via `setOptionString("hwdec", "auto")` in `initOptions()`. (source: RESEARCH-OLD-PLAYER + D-061, 2026-08-03)
- [MISTAKE] Don't clear error state in the `END_FILE` MPV event — `END_FILE` fires normally on quality switch (the old file ends as the new one loads). If you treat `END_FILE` as "error cleared", you'll hide real errors that occurred mid-playback AND you'll race with the next `FILE_LOADED`. Only clear error state on `FILE_LOADED` (the new file successfully started) OR on explicit user action (tap "retry"). The new project's `PlayerObserver` was failing to clear the "Loading failed" overlay because it didn't handle `FILE_LOADED` → the user was stuck on the error overlay even after a successful quality switch. Fix: `onEvent(FILE_LOADED)` calls `updateError(null)` + `updateLoadingState(READY)` + loads tracks. (source: Phase 5c loading-overlay fix, 2026-08-03)
- [CORRECTION] NEVER install the Android SDK, JDK (with javac), or any Android build tooling in the local environment. The user explicitly forbade this. The local environment is for CODE ANALYSIS only — reading files, writing code, using subagents. GitHub Actions provides ALL build tooling. If you need to find a compile error: (1) read the code carefully line by line, (2) use Explore subagents to review, (3) cross-reference against the old project, (4) push to CI and read failure annotations from the GitHub API. NEVER run `./gradlew` locally. NEVER write `sdk.dir=` to `local.properties`. NEVER download `commandlinetools-linux` or `jdk-*-linux-x64_bin.tar.gz`. (source: user correction, 2026-08-03, "never supposed to install Android SDK")
- [MISTAKE] `BorderStroke` is in `androidx.compose.foundation`, NOT `androidx.compose.material3`. Importing from `material3` causes "Unresolved reference 'BorderStroke'" compile error. Always use `import androidx.compose.foundation.BorderStroke`. (source: CI failure 2026-08-04, commit 009dce43)
- [INSIGHT] When CI fails with "Compilation error. See log for more details" and the subagents can't find the error by code review, the ACTUAL error is in the CI build log. Download it via: extract token from `git config --get remote.origin.url`, then `curl -H "Authorization: token $TOKEN" "https://api.github.com/repos/{owner}/{repo}/actions/runs/{id}/logs" -L -o logs.zip`, then `unzip logs.zip && grep "^e:" *.txt`. This is the ONLY reliable way to find the exact error. (source: CI debugging session, 2026-08-04)
- [MISTAKE] When rewriting a Compose file from scratch, ALWAYS check that every Foundation function (`background`, `clickable`, `border`, `clip`, etc.) is imported. The compiler error "Unresolved reference 'X'" usually means a missing `import androidx.compose.foundation.X` statement. Before pushing, grep the file for all function calls and verify each has an import. (source: CI failure 2026-08-04, PlayerSheets.kt missing `background` import)
- [MISTAKE] `Video.url` is DEPRECATED — it returns `videoPageUrl` (the episode page URL), NOT the stream URL. Extensions that use the primary constructor leave `videoPageUrl` empty. ALWAYS use `video.videoUrl` for the actual playable stream URL. The deprecation comment says "Use videoTitle instead" which is misleading — `videoTitle` is the quality label, not the URL. The correct field is `video.videoUrl`. (source: CI debugging session 2026-08-04, log showed `URL: ` empty, root cause of "loading failed")
- [MISTAKE] DOUBLE-RESOLVE BUG: Never call getHosterList (or any extension method that creates local proxy servers) TWICE for the same episode. Extensions like AniKotoS create a new local HTTP proxy on each getHosterList call — the second call kills the first call's proxy. User picks a video from the first resolve (dead proxy) → "loading failed". The old project's ResolverService.kt resolves EXACTLY ONCE and derives both flat + structured results from the same video list. Always use a single resolve() call and pass the raw Videos list to buildServers() for structured derivation. (source: CI debugging 2026-08-04, root cause of "loading failed" — log showed two getHosterList calls with different proxy ports 39369→39073)
- [PATTERN] When a resolver needs to return BOTH a flat list (for a picker sheet) AND a structured/grouped list (for a quality sheet), resolve ONCE and return BOTH in the same state object. Add a `rawVideos: List<Video>` field to the Success state. The caller can then call `buildServers(rawVideos)` synchronously — no second network call. This is exactly what the old project's ResolverService does (single resolveVideoEntries → strategy.resolve derives structured). (source: COMPARE-OLD-TO-NEW + COMPARE-NEW-TO-OLD subagent convergent analysis, 2026-08-04)
- [MISTAKE] When `setSwitching(true)` is called before a video switch, and `updateError()` is wired to SUPPRESS errors while switching (to ignore the old file's END_FILE), ANY real failure during the switch (no videos found, resolve error, exception, server 403, dead proxy) gets suppressed too → the player gets STUCK in a perpetual loading spinner with no error and no recovery path. The fix is to have TWO error methods: `updateError()` (suppressed during switching — for MPV's efEvent/END_FILE only) and `setSwitchingError()` (ALWAYS shown — clears switching flag AND sets error in one call). Use `setSwitchingError` in ALL explicit failure paths (catch blocks, no-videos, resolve-error). Also add a 30s `LaunchedEffect(isSwitching)` watchdog that calls `setSwitchingError("timeout")` if switching stays true for 30s — catches cases where efEvent is suppressed AND FILE_LOADED never fires. (source: D-068 stuck-loading fix, 2026-08-04)
- [PATTERN] WatchKey (Nav3 @Serializable data class) is IMMUTABLE — never try to mutate its fields after an episode switch. Instead, hoist the "current episode" state (url, number, title, resolvedVideosKey) into `PlayerStateHolder` as new StateFlows. Seed them from WatchKey on init, update them on switch. The episode list highlight, "now playing" card, and QualitySheet all read from the state holder (reactive) instead of WatchKey (stale). This is the old project's pattern — it had `currentEpisodeUrl` / `currentEpisodeNumber` on `PlayerStateHolder`. (source: D-069 episode state hoisting, 2026-08-04)
- [MISTAKE] `MPVLib.setPropertyInt("speed", value.toInt())` truncates Float to Int — 1.5f becomes 1, 0.5f becomes 0. For playback speed (and any float MPV property), use `MPVLib.setPropertyDouble("speed", value.toDouble())`. The init-time path correctly uses `setOptionString("speed", speed.toString())` which preserves the float, but the live setter was wrong. (source: D-073 speed setter bug fix, 2026-08-04)
- [MISTAKE] `sub-add` / `audio-add` MPV commands MUST be sent AFTER `FILE_LOADED` — sending them before causes MPV to silently drop the track (no error, just gone). Also, after sending the commands, wait ~300ms before calling `loadTracksFromMpv()` — MPV needs time to register the tracks. Without the delay, the track sheet shows stale data and external subtitles don't appear until the next event. Each `sub-add` triggers an HTTPS download in MPV native code, so run on `Dispatchers.IO`. (source: D-070 external track loading, 2026-08-04)
- [MISTAKE] An EMPTY cacert.pem file is worse than no file. mbedTLS tries to parse it, fails with MBEDTLS_ERR_X509_INVALID_FORMAT (-8576), and does NOT fall back to system CAs → ALL HTTPS streams fail. A MISSING file causes FILE_IO_ERROR which DOES fall back. Always check `file.length() > 0` before setting `tls-ca-file`. Root cause traced from user logs: `tls: mbedtls_x509_crt_parse_file for CA cert returned -8576` → `Failed to open https://...` → `efEvent (load failure): loading failed`. (source: D-075 TLS fix, 2026-08-04)
- [MISTAKE] MPVLib observers (addLogObserver/addObserver) MUST be removed in onDispose (removeLogObserver/removeObserver). Without removal, observers accumulate across screen entries → every event fires N times (N = number of entries). User logs showed 4x duplication after 4 entries. This causes state corruption, error multiplication, and track loading races. Always hoist observer references as state so onDispose can remove them. (source: D-076 observer cleanup, 2026-08-04)
- [MISTAKE] Loading spinner condition must NOT use `!isPlaying` alone. When the user manually pauses, `!isPlaying` is true, and if `loadingState` is still LOADING (e.g. during a seek that triggers START_FILE), the spinner shows — making it look like the video is loading when it's just paused. Correct condition: `buffering || (loadingState == LOADING && duration == 0)` — only show spinner when actually buffering OR during initial load (before video has a duration). (source: D-078 spinner fix, 2026-08-04)
- [MISTAKE] `@Composable` invocations (like `LocalLifecycleOwner.current`) can only happen from a @Composable context — NOT inside a `DisposableEffect` lambda. Call `LocalLifecycleOwner.current` in the composable body, then pass it as a key to `DisposableEffect(lifecycleOwner)`. (source: CI failure 2026-08-04, commit 061c17b)
- [PATTERN] When updating state that's displayed in an overlay (e.g. episode title in EpisodeSwitchingOverlay), update it IMMEDIATELY before the async operation starts — not after it completes. Otherwise the overlay shows stale data during the operation. Example: `updateCurrentEpisode()` must be called before `videoResolver.resolve()`, not after. (source: D-078 episode title fix, 2026-08-04)
- [INSIGHT] When an extension returns garbage data (hashes, URLs, code-like strings as episode names), the app should detect and sanitize it. `EpisodeTitleParser.looksLikeCodeOrHash()` checks for: URL-like strings (starts with / or contains ://), long strings with no spaces (>25 chars), mostly-hex strings (>70% hex chars). Falls back to "Episode N". Also format episode_number: 0 or negative → "?" (bad extension data). (source: D-079 episode sanitization, 2026-08-04)
- [MISTAKE] When switching episodes, the old video keeps playing during the resolve (which takes time). The user hears/sees the old episode while the new one loads. Fix: call `MPVLib.command(arrayOf("stop"))` IMMEDIATELY when the switch starts, BEFORE the resolve. This stops the old video instantly. (source: D-082, 2026-08-04)
- [PATTERN] Separate "error suppression during switch" (isSwitching) from "overlay display" (isSwitchingEpisode). Quality/server switches need error suppression (old file's END_FILE fires) but NOT the "Loading episode..." overlay. Episode switches need BOTH. Using one flag for both causes quality switches to incorrectly show the episode loading overlay. (source: D-082, 2026-08-04)
- [MISTAKE] Auto-retry should NOT clear the error message before re-sending loadfile. If you clear the error, the banner disappears (looks like it auto-dismissed). Instead, keep the error visible, re-send loadfile in the background. If retry succeeds, FILE_LOADED clears the error. If it fails, the error stays. The user said: "it should always show and it should not automatically disappear out of the blue." (source: D-083, 2026-08-04)
- [INSIGHT] Extensions sometimes put timestamps or IDs in `episode_number` (e.g. 1784388992). Real episode numbers are typically 1-100 (up to ~1000 for long-running series). Format numbers > 1000 as "?" to avoid confusing the user with a 10-digit number. (source: D-084, 2026-08-04)
- [INSIGHT] All-caps + digits with no spaces (>10 chars) is almost never a real episode title — it's a code/ID (e.g. "DGFV024L2R0V2IXL0F1"). Detect this pattern in EpisodeTitleParser and fall back to "Episode N". (source: D-084, 2026-08-04)
- [PATTERN] Some HLS formats take a moment to fully parse after FILE_LOADED. The first `loadTracksFromMpv()` might run before MPV has registered all tracks (track-list/count = 0). Fix: schedule a delayed retry (2s) after FILE_LOADED to catch slow-parsing formats. (source: D-085, 2026-08-04)
- [MISTAKE] Never use `|` as a serialization delimiter for episode lists — episode URLs can contain `|` characters (some extensions use `|` in their URL scheme). When the URL contains `|`, `split("|", limit=3)` puts the URL's `|` tail into the episode number field → corrupts the URL, number, AND name. Use `\u001F` (ASCII Unit Separator) instead — it's a control character that never appears in URLs or names. (source: D-087, 2026-08-04)
- [PATTERN] When a module needs a constant that's also used by another module (e.g. `WatchKey` in `:feature:watch:api` needs a delimiter constant that `DetailsScreen` in `:feature:anime-details:impl` also uses), put the constant in a shared module (`:core:common`) to avoid module cycles. `:feature:anime-details:impl` cannot depend on `:feature:watch:api` (would create a cycle). (source: D-087 CI fix, 2026-08-04)
- [MISTAKE] Loading spinner condition must account for quality switches, not just initial load. During a quality switch, `duration > 0` (from the previous video) but the video is being replaced. Condition `loadingState == LOADING && duration == 0` doesn't fire. Add `isSwitching` to the condition: `buffering || isSwitching || (loadingState == LOADING && duration == 0)`. (source: D-088, 2026-08-04)
- [INSIGHT] Switching timeout must account for the resolve phase (network call to extension), not just the loadfile phase. The resolve can take 20-30s. If the watchdog starts when `isSwitching` becomes true (before resolve starts), 30s is not enough. Use 60s, or track two phases (resolving + loading) separately. The old project used 30s but its resolve was pre-done on the details page (instant). (source: D-089, 2026-08-04)
- [MISTAKE] Android 9+ blocks cleartext HTTP traffic by default. Extensions that start local HTTP proxies (e.g. AniKotoS on http://127.0.0.1:PORT) will fail silently — the proxy starts, can't be reached, dies immediately, and all video URLs point to the dead proxy. Symptoms: 60s timeout, "Video failed to load", subtitles not detected, spinner never clears. Fix: add `android:usesCleartextTraffic="true"` + `android:networkSecurityConfig="@xml/network_security_config"` to AndroidManifest, with a network_security_config.xml that allows cleartext for all domains. ALWAYS check this when a new project plays HTTPS fine but HTTP/localhost fails. (source: D-092, 2026-08-04)
- [PATTERN] When comparing old vs new projects to find a regression, ALWAYS compare AndroidManifest.xml first. Network security config, permissions, activity configChanges, service declarations — these are easy to miss and cause silent failures. The old project's manifest is the source of truth for what the new project needs. (source: D-092 investigation, 2026-08-04)
- [MISTAKE] Observing an MPV property is useless if you don't HANDLE it in the observer. `AnikutaMPVView.observeProperties()` observed `seeking` but `PlayerObserver.onProperty()` didn't have a `"seeking" ->` case — so seeking never set `buffering=true`. Always verify every observed property has a handler. (source: D-094, 2026-08-04)
- [MISTAKE] Do NOT call MPVLib.command(arrayOf("stop")) before an episode switch. The old project does NOT stop before switching — it just sends loadfile with "replace" mode. Calling stop first may cause extensions like AniKotoS to detect player disconnection and kill their local proxy. The proxy dies 4ms after starting → all video URLs point to dead proxy → 60s timeout. Fix: just send loadfile("replace") — it stops the old video automatically. (source: D-095, 2026-08-04)
- [PATTERN] For localhost proxy URLs (http://127.0.0.1:PORT/...), do NOT set upstream HTTP headers (Referer, Origin, etc.). The proxy doesn't need them — it forwards requests to the upstream CDN internally. Setting upstream headers on localhost may cause the proxy to reject the connection. Always check if the URL is localhost before setting headers. (source: D-096, 2026-08-04)
- [MISTAKE] `paused-for-cache` may not fire `no` immediately after playback starts. If the spinner condition includes `buffering` (which is set by `paused-for-cache`), the spinner may stay visible after the video starts playing. Fix: also clear `buffering` when `pause = no` (video starts playing). (source: D-097, 2026-08-04)
- [PATTERN] For loading spinners during video switches, don't rely solely on FILE_LOADED to clear the switching flag. Some HLS formats fire FILE_LOADED late or not at all. Instead, use `demuxer-cache-time` to detect when the video has buffered even 1% — once data is flowing, the load succeeded. Condition: `cache-time > position + 1% of duration`. This is more reliable than waiting for FILE_LOADED. (source: D-099, 2026-08-04)
- [MISTAKE] Always render the "Off" entry in subtitle/audio track sheets, even when the track list is empty. The user expects to always be able to turn off subtitles. If "Off" is inside an `else` branch (only shown when tracks exist), the user sees an empty sheet with no way to disable subtitles. Always show "Off" as the first item, then show "no subtitles found" below it if empty. (source: D-100, 2026-08-04)
- [PATTERN] When an investigation sub-agent finds a root cause, verify it by reading the code yourself before implementing the fix. The sub-agent's analysis is valuable but you should confirm the exact lines before changing them. (source: D-100 sub-agent verification, 2026-08-04)
- [PATTERN] When an extension provides data (subtitles, audio tracks) in the `Video` object, carry that data DIRECTLY through the navigation flow (WatchKey/WatchRequest) — don't rely on a registry lookup. The registry lookup can fail (URL mismatch, key blank, registry cleared). The old project carries `subtitleTracks` directly in `WatchRequest`; the new project initially relied on registry lookup and subtitles were lost. Fix: add fields to `ResolvedVideo` (flat type) + `WatchKey`, serialize/deserialize, pass through the navigation callback. (source: D-101, 2026-08-04)
- [INSIGHT] The AniKotoS extension provides subtitles as `Track(url, lang)` pairs in `Video.subtitleTracks`. The URL is a localhost proxy URL like `http://127.0.0.1:PORT/sub/0/0`. The proxy serves the subtitle file with proper headers (Referer for the upstream CDN). This means subtitles require the proxy to be alive — if the proxy dies (cleartext blocked, stop command), subtitles also fail. (source: aniyomi-extensions repo analysis, 2026-08-04)
- [PATTERN] When analyzing extension behavior, clone the extension repo and read the source. The `getHosterList` / `resolveVideo` / `getSubtitleTracks` methods show exactly how data flows. For AniKotoS: `LocalProxyServer.getSubtitleTracks()` returns `List<Track>` with proxy URLs, which are put into `Video.subtitleTracks`. (source: aniyomi-extensions repo analysis, 2026-08-04)
- [MISTAKE] When MPV fires an unexpected event (like event 8 / FILE_ERROR), don't just log it as "Unhandled" and move on. Check if the video is actually playing (PLAYBACK_RESTART fires, time-pos advances, video decoder shows FPS) — if it is, the file loaded despite the error event. Always handle PLAYBACK_RESTART (17) as a fallback for FILE_LOADED (11) — it fires when playback actually starts and can be used to trigger subtitle loading when FILE_LOADED is missed. (source: D-109, 2026-08-05)
- [PATTERN] When comparing old vs new project logs to find a bug, compare the EXACT sequence of MPV events. The old project gets FILE_LOADED (11) → sub-add → subtitles loaded. The new project gets FILE_ERROR (8) + PLAYBACK_RESTART (17) but never FILE_LOADED. The fix is to handle PLAYBACK_RESTART as a fallback. Always diff the event sequences — the missing event is the bug. (source: D-109, 2026-08-05)
- [INSIGHT] Event 8 in libmpv 1.18 is NOT the same as in older versions. In older libmpv, event 8 is FILE_ERROR (deprecated). In newer versions, it might be a different event. Always verify event ID mappings against the actual MPV library version being used. Don't trust hardcoded event name mappings — use `MPVLib.mpvEventId.*` constants when available. (source: D-109 investigation, 2026-08-05)
- [MISTAKE] `Modifier.fillMaxSize()` on a child of a wrap-content-height `Box` resolves to **0 height** (the incoming max-height constraint is unbounded/Infinity, so fillMaxSize falls back to the min constraint = 0). This makes the child invisible. For a background layer behind a card in a `Box`, use `matchParentSize()` (a `BoxScope` modifier) — it sizes to the Box's footprint *after* the size-defining sibling is measured. This was the "swipe background gone" bug: the prior session swapped matchParentSize → fillMaxSize based on the false theory that "matchParentSize fails for siblings." matchParentSize is *designed* for siblings — it's a BoxScope member. (source: D-153, 2026-08-08)
- [MISTAKE] When a @Composable function emits multiple top-level composables (e.g. a toggle `Row` + a content `LazyColumn`) and is called inside a parent `Box`, those siblings **stack in z-order** (declaration order; later draws on top). A `fillMaxSize` content declared after a toggle will draw ON TOP of the toggle, hiding it. This was the "can't click the calendar button" bug — the list covered the toggle. Fix: wrap the toggle + content in a `Column` (vertical stacking) so neither covers the other. Rule: if a composable emits a control + its content, wrap them in a layout (Column/Box-with-alignment), don't emit them as bare siblings. (source: D-154, 2026-08-08)
- [MISTAKE] Never trust a self-reported "CI green" in docs. The previous session's `progress.md` claimed "CI green" but the last 2 commits (`db26c47`, `fd1a9a5`) had actually FAILED CI (`:app:compileDebugKotlin` — DocumentFile unresolved). Always poll the GitHub Actions run (`/actions/runs/{id}` → check `conclusion == "success"`) and read failure annotations/logs before claiming green. A docs-only commit failing the APK build = a pre-existing compile error, not a docs problem. (source: D-156, 2026-08-08)
- [PATTERN] Gradle `implementation` dependencies are NOT transitively visible to consumers (only `api` is). If module A depends on B via `implementation`, and module C depends on A, then C cannot directly reference B's classes — C must declare its own `implementation(B)`. This bit us when `:app` used `DocumentFile` directly: `:core:download` had `implementation(libs.androidx.documentfile)` but `:app` (which depends on `:core:download` via implementation) couldn't see `DocumentFile`. When porting code that uses a library into a new module, check that module's `build.gradle.kts` for the dep — don't assume transitivity. (source: D-156, 2026-08-08)
- [MISTAKE] Enum companion-object methods can't reference enum instances via `this` — inside `companion object`, `this` is the companion, NOT an enum value. If you write `fun subBoolean(): Boolean = this != DUB` inside the companion, it won't compile ("Operator != cannot be applied to Companion and Enum"). Instance methods (defined on the enum body, outside the companion) see `this` as the instance. Rule: methods that branch on the enum value belong on the enum BODY, not in the companion. The companion is for factories/static helpers only. (source: D-158, 2026-08-08)
- [MISTAKE] `var x by remember { mutableStateOf(v) }` needs BOTH `getValue` and `setValue` operator-extension imports for `MutableState` (from `androidx.compose.runtime`). `val x by remember { mutableStateOf(v) }` only needs `getValue`. Forgetting `setValue` for a `var` delegate gives "Type MutableState has no method setValue… cannot serve as a delegate for var." Always import both when using `var ... by remember { mutableStateOf }`. (source: D-158, 2026-08-08)
- [MISTAKE] `AnimatedVisibility` has a `ColumnScope` extension overload AND a standalone top-level function. Inside a `LazyColumn` `item {}`, the receiver is `LazyItemScope` (NOT `ColumnScope`), so the `ColumnScope.AnimatedVisibility` "cannot be called in this context with an implicit receiver." Fix: wrap the item content in a `Column { AnimatedVisibility(...) }` so a `ColumnScope` is in scope. (source: D-159, 2026-08-08)
- [PATTERN] When a child composable needs to trigger an action in a descendant that owns internal state (e.g. a "Today" button in a toggle bar that must scroll a `HorizontalPager` living inside a child composable), use a **request-counter** `MutableState<Int>` passed down + observed via `LaunchedEffect(counter)`. Don't try to hoist the descendant's state (pagerState) up — that couples the parent to the child's internals. The counter increments on each request; the child's LaunchedEffect fires + performs the action. Cleaner than passing a lambda because the child retains ownership of the state. (source: D-157, 2026-08-08)
- [PATTERN] To upgrade a boolean DB column to tri-state without a migration: keep the column as INTEGER, reinterpret 0/1/2 as the three states, and map old 0/1 data to two of the states (backward compatible — `CREATE TABLE IF NOT EXISTS` won't try to alter an existing column). New state = 2. The `TriggerState` enum's `fromDb(value)` maps anything unknown to the OFF default. This avoids SQLite ALTER TABLE (which can't change column type without a table rebuild). (source: D-158, 2026-08-08)
- [MISTAKE] SharedPreferences stores each value with its TYPE (Boolean, Int, Long, Float, String, Set<String>) and does NOT auto-convert. If a key was written as Boolean in a previous build and you change the code to read it as Int (`prefs.getInt(key)`), it throws `ClassCastException: java.lang.Boolean cannot be cast to java.lang.Integer` at runtime — a hard crash, not a default fallback. This bit us when D-158 upgraded the notification trigger defaults from Boolean to Int but kept the same keys; devices with the old build crashed ~1s after opening the Notifications page (when the flow collected + called getInt). FIX: when changing a pref key's type, EITHER (a) use a new key (easiest, loses old data — fine for defaults), OR (b) migrate defensively in an `init` block: `try { store.getInt(key, 0) } catch (ClassCastException) { val old = store.getBoolean(key, false); store.putInt(key, if (old) 1 else 0) }`. Idempotent + safe. Rule: SharedPreferences type changes require a migration — there is no implicit conversion. (source: D-161, 2026-08-09)
- [INSIGHT] SQLite UPSERT (`INSERT ... ON CONFLICT(...) DO UPDATE SET ...`) requires SQLite 3.24+ (2018). Android API 24-28 (Android 7-9, our minSdk=24) ships SQLite 3.9-3.22 which does NOT support it. Since we use the system SQLite via `AndroidSqliteDriver`, we CANNOT use UPSERT without breaking older devices. `INSERT OR REPLACE` is the workaround — it deletes + reinserts the row (rowid churn), but the callers already read-then-write so the fragility is mitigated. Always check the SQLite version constraint on minSdk before adopting new SQL syntax. (source: D-166, 2026-08-10)
- [MISTAKE] Extension trust by-signing-certificate-fingerprint is a SECURITY gate (correct — only trusted signers can load), but it auto-propagates: trusting ONE extension from a repo auto-trusts ALL extensions from the same signer on the next `loadAll()`. The user reported "I trusted 2 but all show when resolving." FIX: add a per-package `isEnabled` flag INDEPENDENT of signer-level trust. Only enabled extensions' sources appear in pickers. Trust = signer (security); Enabled = package (user control). Backward compat: seed the enabled set with all currently-trusted pkgNames on first launch. (source: D-168, 2026-08-10)
- [PATTERN] When an enriched cache write overwrites a field (e.g., AniList title overwriting extension episode name), preserve the original via a lookup map. Pattern: `val epNumToOriginal = episodes.associate { it.episode_number.toInt() to it.originalField }`, then in the enriched write: `originalField = epNumToOriginal[epNum]`. This is the same pattern as `epNumToUrl` (D.FIX). Always preserve the extension's original data through the AniList enrichment — the two serve different purposes (extension data for audio parsing, AniList data for display). (source: D-167, 2026-08-10)
- [MISTAKE] When adding a UI element (e.g., StarRatingBar) inside a composable function that's NOT the top-level screen composable (e.g., `MinimizedMode` inside `WatchScreen`), the variables declared in the top-level composable (`ratingStore`, `episodeRating`, `scope`) are NOT in scope. Either pass them as parameters, or declare them locally inside the child composable (using `koinInject` + `rememberCoroutineScope`). Always check which composable function your code is in before referencing variables from the outer scope. (source: D-170 CI failure, 2026-08-10)
- [PATTERN] When a `setAutoMarkSuppressed`-style UPDATE query is called on a row that might not exist yet, add an INSERT-when-missing guard: `val existing = queries.get(key).executeAsOneOrNull(); if (existing == null) { queries.upsert(...) } else { queries.update(...) }`. This prevents silent no-ops when the user interacts with an episode they never played. The same pattern was already used in `setUserMarkedWatched` — always apply it to all UPDATE-only methods that operate on optional rows. (source: D-169 WP-B1, 2026-08-10)
- [MISTAKE] `AnimatedVisibility` has THREE overloads: the top-level `@Composable fun AnimatedVisibility(...)`, `RowScope.AnimatedVisibility(...)`, and `ColumnScope.AnimatedVisibility(...)`. When you call `AnimatedVisibility(...)` inside a `Box` that is itself inside a `Row`, the chart Row's `RowScope` receiver LEAKS INTO the Box's content lambda (Kotlin nested receivers are all accessible). The compiler resolves to `RowScope.AnimatedVisibility` and rejects it: "cannot be called in this context with an implicit receiver" (because the innermost receiver is `BoxScope`, not `RowScope`, and the extension can't be dispatched). This is the SAME class of bug as D-159 (LazyItemScope vs ColumnScope). FIX: extract the `AnimatedVisibility` + its content into a separate top-level `@Composable` function (no scope receiver in its body) and call IT from inside the Box, passing `modifier = Modifier.align(...)` through. The previous session worked around this by replacing `AnimatedVisibility` with a plain `if` — losing the animation. The extraction fix preserves the animation. Rule: if `AnimatedVisibility` is called inside a Box-within-a-Row (or Box-within-a-Column), extract it into a scope-less composable. (source: D-176, 2026-08-10)
- [PATTERN] For smooth scroll-driven UI shrink/grow animations, drive `graphicsLayer` directly from a `() -> Float` lambda that reads `LazyListState.firstVisibleItemScrollOffset` / `firstVisibleItemIndex` — this is a DEFERRED read (no recomposition on scroll, GPU-only). Do NOT use a boolean `collapsed` toggle + `animateFloatAsState` for the shrink itself — that produces a stepped/two-phase animation (the boolean flips at a threshold, then animates) which looks "jumpy." The boolean+animateFloatAsState pattern is fine for properties that CAN'T be deferred (like `CollapsingHeader`'s font size, which is a layout property), but for alpha/scale/translation, always prefer the continuous graphicsLayer-lambda approach. (source: D-171, 2026-08-10)
- [PATTERN] When a scroll item should "shrink and disappear" as the user scrolls, make it a real `LazyColumn` item (item 0) rather than a pinned box that height-animates. A pinned box that height-animates to 0 causes layout reflow of the LazyColumn below (potential jank) + leaves the question of what scroll position aligns the next item to the top. A real scroll item scrolls away naturally — the next item lands at the top with zero extra logic. Combine with `graphicsLayer { alpha/scale driven by scroll offset }` for the shrink visual. (source: D-171, 2026-08-10)
- [PATTERN] `ActivityResultContracts.PickVisualMedia()` returns a `content://` URI that is ONLY valid for the current session (the photo picker does NOT grant persistable URI permission, unlike `OpenDocumentTree`). To make a picked avatar persist across app launches, copy the URI to app-internal storage (`context.filesDir/avatar_<ts>.jpg` via `contentResolver.openInputStream(uri)?.use { it.copyTo(dest.outputStream()) }` on `Dispatchers.IO`) and store the resulting `file://` path. Coil loads `file://` URIs directly. (source: D-175, 2026-08-10)
- [MISTAKE] In Coil 3.0.4, `coil3.request.Success` is NOT a public top-level class — `import coil3.request.Success` gives "Unresolved reference." The `ImageResult` sealed interface exposes `image: Image?` directly (nullable — null on error, non-null on success). Access it via `result.image` without pattern-matching to `Success`. The correct way to get a Bitmap: `val result = loader.execute(request); result.image?.let { (it as? coil3.BitmapImage)?.bitmap }`. Don't assume the sealed subtypes are top-level — check the actual API. (source: D-182, 2026-08-10)
- [MISTAKE] `kotlin.math.min(a, b)` only works on primitive types (Int, Long, Float, Double). For Compose `Dp` (which implements `Comparable<Dp>` but is not a primitive), use `minOf(a, b)` — Kotlin's `minOf` uses the `Comparable` overload. This also applies to `maxOf` for `Dp`. (source: D-182, 2026-08-10)
- [PATTERN] Local properties with custom getters (`val x: String get() = ...`) have restrictions inside `@Composable` functions — the compiler may reject them with "Syntax error: Unexpected tokens" or "Variable must be initialized." Use a regular `val x = if (...) ...` instead — it's recomputed on each recomposition (when the underlying `mutableStateOf` values change), which is the desired behavior. Local delegated properties (`val x by remember { ... }`) work fine; the restriction is specifically on local properties with explicit `get()` accessors. (source: D-182, 2026-08-10)
- [PATTERN] For a Compose image crop editor: load the full bitmap via Coil's `ImageLoader.execute().image`, display it with `ContentScale.Crop` + `graphicsLayer(scaleX, scaleY, translationX, translationY)` for zoom/pan, detect gestures with `detectTransformGestures { _, pan, zoom, _ -> }`, and crop via `Bitmap.createBitmap(bmp, left, top, size, size)` where the source rect is computed by mapping the display transform back to bitmap coordinates: `centerInBmp = (iw/2 - offset.x / (baseScale * userScale), ih/2 - offset.y / (baseScale * userScale))`, `sourceSize = framePx / (baseScale * userScale)`. Clamp the offset during pan: `maxOffset = (iw * effectiveScale - framePx) / 2`. (source: D-181, 2026-08-10)
- [MISTAKE] A scroll-end "magnetic snap" effect (`snapshotFlow { isScrollInProgress }.filter { !it }.collect { snap }`) must be guarded by a position check. If the snap fires on EVERY scroll end regardless of scroll position, scrolling deep into content triggers `animateScrollToItem(1, 0)` — jumping the user back to the top. Always guard: `if (listState.firstVisibleItemIndex == 0) { snap }` — only snap when the user is near the top (in the threshold area). The snap is a "magnetic" effect for the tab-bar/header area, NOT a global scroll-position enforcer. (source: D-183, 2026-08-10)
- [PATTERN] For a floating overlay (sidebar, popover) that should close on tap-outside, place a transparent scrim `Box(Modifier.fillMaxSize().clickable { dismiss() })` as a sibling BEFORE the overlay in the parent `Box`. The scrim captures all taps on the parent area; the overlay (declared after the scrim) draws on top and consumes its own taps via `.clickable { /* consume */ }`. This is more reliable than putting `.clickable { dismiss() }` on the parent Box directly (which can interfere with child clickables) or on a sub-Box that doesn't cover the full area. (source: D-184, 2026-08-10)
- [PATTERN] Before merging a feature branch, always check if `main` has diverged: `git log --oneline feature..origin/main`. If the count is 0, the merge is a clean fast-forward (`git merge --ff-only`) — no merge commit, no conflict resolution needed. If main HAS diverged, a merge commit (or rebase) is needed and conflicts are possible. Always `git fetch origin main` first to get the latest remote state. The fast-forward-only flag (`--ff-only`) is a safety net: it aborts if a fast-forward isn't possible, preventing an accidental merge commit when you intended a fast-forward. (source: merge session, 2026-08-10)
- [PATTERN] Branch deletion order after a merge: (1) verify CI green on `main` FIRST (the merged code builds on the target branch), (2) delete the remote branch (`git push origin --delete <branch>`), (3) delete the local branch (`git branch -d <branch>` — the `-d` flag refuses to delete if the branch isn't fully merged, a safety net). Never delete the feature branch before CI on main is green — if main's build fails, you'd lose the ability to easily iterate on the feature branch. (source: merge session, 2026-08-10)
- [MISTAKE] Documentation can drift from code in TWO directions: (a) docs describe a state that was never built (e.g. `knowledge/architecture.md` proposed 8 modules + Room; actual 46 + SQLDelight — never updated after Phase 1), (b) docs describe a state that was REMOVED (e.g. 4 docs said "Nav3 on classpath unused" but Nav3 had been fully removed from all build files — the docs were stale by a full removal). Direction (b) is more dangerous because the docs claim something EXISTS when it doesn't — the next session trusts the doc + wastes time looking for code that's gone. RULE: when removing a dependency/feature, grep ALL docs (not just the one you're editing) for references to it. The `knowledge/*` files are especially prone to drift because they're "summaries" that nobody re-derives from source. A periodic doc-debt sweep (verify every knowledge/ file against actual code) catches both directions. (source: doc-debt-sweep session, 2026-08-10, Nav3 removed but 4 docs still referenced it + all knowledge/ files proposed 8 modules)
- [PATTERN] When a project review reveals doc-drift, fix ALL of it in one session — don't "fix the urgent ones + defer the rest." Partial fixes leave the doc-base inconsistent (some files current, some stale), which is WORSE than uniformly stale because the next session can't trust ANY file. Either sweep all docs to current, or flag explicitly which are stale + which are current. The `progress.md` → "Known doc debt" section is the canonical place to track which docs are current vs stale. (source: doc-debt-sweep session, 2026-08-10)
- [INSIGHT] A "Deferred Concerns" registry (a table in `progress.md` with: # / concern / severity / est. effort / dependencies) is the right structure for tracking issues the user has seen but deferred. It's better than scattering "TODO" comments in code (which are invisible from the project-overview level) or adding them to `decisions.md` (which is for confirmed decisions, not unfixed issues). The registry gives the user a single place to see "what's known but not fixed" + pick the next item to tackle. Each item should reference the relevant D-NNN if one exists. (source: doc-debt-sweep session, 2026-08-10, D-188)
- [MISTAKE] Enabling `PRAGMA foreign_keys = ON` (D-166) on a database with EXISTING data exposes EVERY latent FK violation that was silently dangling when FKs were OFF. Before enabling FK enforcement, audit EVERY FK in the schema: (a) is the parent table populated? (b) does the code actually insert parent rows before child rows? (c) does the code pass the correct parent ID (not a different-domain ID that happens to be a Long)? The D-189 crash was caused by `content.extension_id` having a FK to `content_ext(id)`, but `content_ext` was never populated + the code passed `source.id` (Aniyomi internal) as `extensionId` — a semantic mismatch that was invisible pre-D-166. RULE: when enabling FK enforcement on an existing schema, grep every FK column's write sites + verify the value being written is a real parent-row ID (not a different-domain ID). (source: D-189, 2026-08-12, FK enforcement crash on link-source)
- [PATTERN] When a lookup/parent table (like `content_ext`) has an `insertX`/`getOrCreateX` method but ZERO callers, the table is dead. Any FK referencing it from a populated table will crash once FK enforcement is ON. Two fixes: (a) wire up the table (call getOrCreate at every write site) — the "code matches schema" fix; (b) drop the FK (keep the column as a plain INTEGER) — the "schema matches code" fix. Choose (b) when the code has ALWAYS used a different ID domain (e.g. Aniyomi source.id vs content_ext.id) — the FK was wrong from the start. Choose (a) when the code is halfway-converted (some sites use the parent ID, some don't). The D-189 fix chose (b) because all 6 write sites consistently passed source.id. (source: D-189, 2026-08-12)
- [INSIGHT] SQLite `CREATE TABLE IF NOT EXISTS` does NOT alter an existing table's schema (it's a no-op if the table exists). This means schema changes to an existing table (adding/removing FK constraints, changing column types) do NOT take effect on existing installs — only on fresh installs (or after `DROP TABLE` + recreate). Per CORE_RULES §30 (debug = clear data / reinstall), this is acceptable for debug. For production, you need either `.sqm` migration files (SQLDelight) or an `onOpen` table-rebuild (CREATE new → INSERT SELECT → DROP old → RENAME). Don't assume editing the `.sq` file fixes existing installs — tell the user to reinstall. (source: D-189, 2026-08-12)
- [PATTERN] When building a multi-source metadata engine, use a pluggable provider interface with `supportedIdTypes: Set<ContentIdType>` rather than hardcoding the ID type. This makes the engine future-proof: adding a new ID type (e.g. TMDB) = a new provider module that declares its supported types — zero engine changes. The engine auto-selects providers based on the content's `ContentId.type`. D-190 used this pattern for AniZip (ANILIST+MAL) + Jikan (MAL) + Kitsu (ANILIST); a future TMDB provider would declare `{TMDB}`. (source: D-190, 2026-08-12)
- [PATTERN] When fetching from multiple providers in parallel with `coroutineScope { map { async { ... } }.awaitAll() }`, each `async` block MUST wrap its body in `try { ... } catch (e: Exception) { emptyMap() }`. Without per-async try/catch, one provider's exception propagates through `awaitAll()` → cancels the parent `coroutineScope` → all sibling in-flight providers are cancelled. This defeats the purpose of multi-source: you lose ALL data because ONE source failed. The pattern: `async { try { provider.fetch() } catch (e: Exception) { Logger.w(...); emptyMap() } }`. (source: D-190 sub-agent plan review, 2026-08-12)
- [INSIGHT] For boolean fields that come from only ONE source (e.g. `isFiller`/`isRecap` from Jikan only), use `Boolean?` (nullable) — NOT `Boolean = false`. Null means "unknown" (the source didn't report); false means "confirmed-not". If the source fails (network error, rate limit), the field stays null — UI shows no badge. If you default to false, the UI would show "non-filler" even though you don't actually know — misleading for long anime where Jikan pagination fails partway through. The merge strategy: OR-true across sources (if any says true, it's true; false-wins over null; null only if all null). (source: D-190, 2026-08-12)
- [MISTAKE] AniZip's show-level `titles.x-jat` is unreliable (returns "NTV" for Naruto — clearly a placeholder/bug). But the per-episode `title.x-jat` is correct. When extracting romaji titles, ALWAYS use the per-episode path, not the show-level path. Verify API response shapes with live test calls before trusting a field — don't assume the show-level and per-episode paths have the same data quality. (source: D-190 sub-agent plan review, 2026-08-12, AniZip API verification)
- [PATTERN] Jikan v4 API responses often have trailing NBSP (`\u00A0`) in `title_romanji` fields. Always `.trim()` Jikan string fields on deserialization — `trim()` removes NBSP (U+00A0) along with regular whitespace. Don't store untrimmed strings in the DB — they cause invisible display glitches (trailing space in episode titles). (source: D-190, 2026-08-12, Jikan API verification)
- [MISTAKE] When writing a test checklist for a feature that requires prerequisites (extensions, login, network, etc.), STATE THE PREREQUISITES EXPLICITLY at the top of the relevant phase — don't assume the user knows. The D-191 test checklist had Phase 5 "watch an episode" without saying extensions are required. The user correctly flagged this: episodes can't load/resolve/play without a trusted extension. RULE: every test phase should have a "Prerequisites:" line listing what must be true before starting (e.g. "Prerequisites: at least one extension installed + trusted"). Don't make the user guess. (source: D-191, 2026-08-12, user correction on Phase 5)
- [INSIGHT] When analyzing a DB export for "is the DB healthy?", check 4 things in order: (1) row counts per table — empty tables that should have data = feature not wired; (2) FK orphans — child rows whose parent_id doesn't exist in the parent table; (3) NULL fields in NOT-NULL-expected columns — silent data loss; (4) write patterns in the DB activity log — tables with zero writes = feature never writes. The D-191 analysis found `activity_event` empty via check #1 + #4 (zero writes, zero rows — confirmed `ActivityTracker.track()` has zero callers). A DB export + activity log together are more diagnostic than either alone. (source: D-191, 2026-08-12, DB analysis session)
- [PATTERN] When a feature table is empty but the feature appears to work (e.g. `user_customization` empty but appearance settings persist), the feature is likely using a DIFFERENT storage layer (SharedPreferences/PreferenceStore instead of the DB table). This is a sign of storage-layer inconsistency — some settings in DB, some in prefs. Audit which layer owns which setting; either consolidate to one layer or document the split. Don't leave a dead DB table — it confuses the next developer. (source: D-191, 2026-08-12, user_customization empty)
- [MISTAKE] SQLDelight column names that are Kotlin soft keywords (`key`, `value`, `type`, `class`, `fun`, etc.) cause "Unresolved reference" compile errors. SQLDelight generates a data class property with the column name — if the name is a Kotlin keyword, the property can't be accessed without backtick escaping (ugly + error-prone). RULE: prefix column names to avoid keywords (e.g. `setting_key` not `key`, `setting_value` not `value`). This is why the `app_settings` table uses `setting_*` prefixed columns. (source: D-192, 2026-08-12, CI failure on first attempt)
- [PATTERN] When dropping a SQLDelight table, the cleanup touches 4 layers: (1) the .sq file (remove CREATE TABLE + indexes + queries), (2) the DatabaseDriverFactory onOpen (add DROP TABLE IF EXISTS), (3) the Repository class (remove methods that called the queries), (4) any data classes only used by those methods. Grep for ALL references to the removed query names + method names before pushing to CI. A missed reference = compile error. (source: D-192, 2026-08-12, dead table cleanup)
- [INSIGHT] When a user lists "dead tables to drop," VERIFY each one before dropping. The user may conflate "empty table" with "dead table." An empty table might be ACTIVE (code writes/reads it, but no data has been produced yet due to an upstream bug). D-191/D-192: user listed notification_config, notification_sent, episode_update, download_queue as "dead" — research showed all 4 are ACTIVE (code writes + reads them). Dropping them would break built features. RULE: "empty" ≠ "dead". Check for writes (INSERT/UPDATE queries) AND reads (SELECT queries) before dropping. (source: D-192, 2026-08-12, user listed 8 dead tables, only 3 were actually dead)
- [PATTERN] When an "ensureX" method (like `ensureUpdateState`) exists but is never called, the feature it supports is silently broken. The method was designed to be called on a specific user action (library-add) but the wiring was forgotten. ALWAYS grep for callers of "ensure" methods after implementation — if zero callers, the feature is broken. (source: D-192 Phase 3, 2026-08-12, Updates feed empty because ensureUpdateState was never called)
- [INSIGHT] A "loadGeneration" counter is the simplest fix for stale-state flash in a ViewModel that loads content sequentially. Increment a counter on every load. Capture the generation at the start of each async block. Before writing state, check if the generation is still current. This prevents async results from a previous content load from overwriting the current content's state. The pattern is lightweight (one int field) + doesn't require cancelling coroutines (which is fragile in Compose). (source: D-192 Phase 5, 2026-08-12)
- [MISTAKE] When a data class is constructed in multiple places (e.g. DownloadedEpisode in DownloadQueue + DownloadScanner), adding new fields with default values is safe — existing construction sites use the defaults. But the DATA LOSS happens when the DB insert hardcodes the new fields to null instead of passing the data class's values. ALWAYS check the insert/store layer — not just the data class. The schema may have the columns, the data class may have the fields, but the store may be dropping the data by hardcoding null. (source: D-192 Phase 4, 2026-08-12, DownloadedEpisode data-loss)
- [MISTAKE] Merging a feature branch to `main` without the user's explicit confirmation violates workflow.md ("Do NOT merge to `main` until the user explicitly says to. The user verifies on device first."). Even if CI is green + the change seems safe, the MERGE decision is the user's — not the agent's. The agent's job is: create branch → push → verify CI → WAIT for user to say "merge". Getting into a "CI green → merge → next phase" rhythm skips the verification gate + erodes user trust. RULE: never merge to main without an explicit "merge" / "ship it" / "go ahead" from the user. A green CI is necessary but not sufficient. (source: D-192, 2026-08-12, user feedback on merging 5 phases without confirmation)
- [MISTAKE] Documenting a system "as specified" instead of "as implemented" creates false claims. The D-193 doc web page said "episode-type toggle gates notifications only" + "library customization toggle exists" — but the code did the opposite (toggle gated checking) and the toggle didn't exist. RULE: ALWAYS audit the actual code before documenting behavior. A doc that overstates reality is worse than no doc — it misleads the user into thinking features work. (source: D-193 v2, 2026-08-12, doc web page vs code mismatch)
- [PATTERN] When moving a gating decision from one layer to another (e.g. sub/dub toggle from engine → NotificationManager), inject the preferences into the new layer rather than threading the decision through call-site params. NotificationManager already had NotificationPreferences; adding UpdatePreferences as a 5th constructor param (nullable for testability) was the clean seam. (source: D-193 v2, 2026-08-12)
- [PATTERN] For "smart learning" features (averaging, offset tracking), store the learned value in a dedicated DB column — don't reuse an existing column or compute it on the fly. The learned_offset_ms column lets the system persist the average across app restarts + converges over multiple episodes. Computing it from the latest single observation (the old approach) is "replacing" not "averaging" — it chases noise. (source: D-193 v2, 2026-08-12, smart-release averaging)
- [PATTERN] When a sub-agent compile review finds "missing import" issues, those are mechanical + reliable to fix. When it finds "missing Gradle dependency" issues, those are high-value catches — `implementation` deps don't transitively expose to downstream modules on the compile classpath, so a new reference to a class in a non-depended module compiles locally (if the agent doesn't check) but fails on CI. ALWAYS cross-check new class references against the module's build.gradle.kts deps. (source: D-193 v2 review-1, 2026-08-12, :core:preferences not in :feature:updates:impl)
- [PATTERN] When CI fails with "Unresolved reference" for a NEW screen/composable, the most likely cause is a missing import in the dispatch file (e.g. MainActivity). The sub-agent review caught missing imports in non-dispatch files, but the dispatch file's import for the new screen was missed because the screen is in a different package. RULE: after creating a new screen, immediately add its import to every file that dispatches to it — don't wait for CI. (source: D-193 v2, 2026-08-12, CI run 31634281699 failed on UpdateCategoriesScreen import)
- [INSIGHT] Doc-drift compounds silently: D-192 Phase 1 DROPPED 3 dead tables (`content_ext`, `content_ext_repo`, `user_customization`), reducing the table count 28 → 26 — but EVERY doc/data file still says "28 tables" (master.md, SESSION.md, knowledge/architecture.md, knowledge/dashboard.md, knowledge/tech-stack.md, lib/data.ts, Footer.tsx, decisions.md). A fresh codebase review this session caught it by actually counting `.sq` files + tables. Reinforces CORE_RULES §26: after a structural change (table drop), grep ALL docs + dashboard data for the old count + update in the same session — not "later". Also: the worklog's own claim of "actual 28" (Task 3-a) was itself stale because it read the stale docs. ALWAYS re-derive counts from the actual `.sq` files, not from docs. (source: self, 2026-08-12, project review caught 26 vs 28)
- [PATTERN] In the Next.js dashboard, hero badges with long text + `whitespace-nowrap` + fixed `h-7` cause horizontal overflow on mobile (375px viewport). The "Reviewed this session · D-193 v2 Updates + Notifications merge · commit 57bbd17" badge was 394px wide, overflowing by 56px. FIX: use the dashboard's standard pill pattern — NO `whitespace-nowrap` (other pills like the metric row + decisions filter pills don't use it + wrap fine), + `py-1.5` instead of fixed `h-7` for flexible height when text wraps. RULE: never put `whitespace-nowrap` on a badge whose text could exceed ~300px on mobile; verify with Agent Browser at 375px viewport after building. (source: self, 2026-08-12, project-review hero badge mobile overflow fix 5b8351ef)
- [INSIGHT] When verifying a deployed GitHub Pages site via Agent Browser, the page title can come back empty on first poll if the page is a client component using `useState` (e.g. `/decisions/` page). Wait longer (`wait --load networkidle` + `wait 2000`) before asserting content. Server-rendered pages (like the new `/project-review/`) render the title immediately. Don't conclude "page broken" from a single fast poll. (source: self, 2026-08-12, decisions page title empty on first poll, fine on second)

- [PATTERN] Custom `ImageVector` icons can be built without `material-icons-extended` by using `ImageVector.Builder` + the top-level `path(...)` extension function on the builder. Use only basic `PathBuilder` commands (`moveTo`, `horizontalLineTo`, `verticalLineTo`, `cubicTo`, `close`) — avoid `arcTo` because its parameter names (`horizontalEllipseRadius`/`verticalEllipseRadius` vs `rx`/`ry`) vary across Compose versions. Draw icons as filled shapes (not strokes) for legibility at 8–10dp. (source: D-242-fix14, 2026-08-22, BadgeIcons.kt)
- [PATTERN] When migrating a Compose composable's parameter type (e.g. `Pair<String, Pair<Color, Color>>` → a data class with an optional icon), grep for ALL call sites — not just the one you're editing. The old `CoverBadgeRow` had only one caller, but if there had been others, they would silently fail to compile. A data class with named fields is always safer than nested Pairs for evolving APIs. (source: D-242-fix14, 2026-08-22, CoverBadgeData migration)
- [PATTERN] The scroll-to-minimize header pattern (from ProfileScreen) can be replicated in a `ModalBottomSheet` by putting the tab strip as item 0 in a `LazyColumn` + applying `graphicsLayer { alpha = (1f - scrollFraction); scaleX = 1 - f*0.25; scaleY = ... }`. The collapse threshold should match the actual tab strip height (~40dp for a 2-tab strip with 8dp vertical padding). Magnetic snap via `snapshotFlow { listState.isScrollInProgress }.filter { !it }.collect { animateScrollToItem(...) }`. (source: D-242-fix13/14, 2026-08-22, CustomizeSheet)
- [MISTAKE] When a badge fallback uses `isEmpty()` to decide whether to show a default value, check whether the list is empty because data is MISSING (null) vs because data is ZERO. In D-242-fix14, the BOTH+unwatched fallback showed "EP N" when the user had watched everything (both per-type unwatched counts = 0) — contradicting the "unwatched only" preference. Fix: guard the fallback with an explicit null check (`hasPerTypeData = subCount != null || dubCount != null`), not an emptiness check. (source: D-242-fix14 review, 2026-08-22, sub-agent 8-c caught this)
- [INSIGHT] Sub-agent parallel review is highly effective for Compose UI changes: 3 agents reviewing different aspects (data layer, sheet UI, badge rendering) of the same commit all found NO compile issues, but one caught a subtle logic bug the others missed. The logic bug (fallback firing on zero-count) would have been visible only at runtime with specific data states. Always include at least one reviewer focused on edge-case logic, not just compile safety. (source: D-242-fix14, 2026-08-22, 3-agent review)
- [BLOCKER] GitHub Actions CI builds require a push to the remote, which requires GitHub credentials (PAT or SSH key). If the build environment has no credentials (`gh auth status` fails, no `~/.git-credentials`, no `GITHUB_TOKEN` env var), the push cannot be automated. The commit is saved locally and ready to push — the user must push from an authenticated environment. Use `ntfy.sh` to notify the user of this blocker. (source: D-242-fix14 session, 2026-08-22)

- [MISTAKE] Compose's `PathBuilder` (in `androidx.compose.ui.graphics.vector`) uses `curveTo(FFFFFF)` for cubic bezier curves — NOT `cubicTo`. The `cubicTo` method exists on `AndroidPath` (`androidx.compose.ui.graphics.AndroidPath`) but NOT on `PathBuilder`. If you're building a custom `ImageVector` via `ImageVector.Builder { path { ... } }`, the lambda receiver is `PathBuilder`, so you must use `curveTo`. To verify the correct API name for any Compose version, grep `baseline-prof.txt` in the references: `PathBuilder;->curveTo(FFFFFF)V`. (source: D-242-fix14-ci, 2026-08-22, CI run 32587888021 failed with "Unresolved reference 'cubicTo'")
- [PATTERN] When a CI build fails on a compile error that sub-agents didn't catch, check whether the API name differs between similar classes. Sub-agents review against their training knowledge, which may conflate `PathBuilder.cubicTo` (doesn't exist) with `AndroidPath.cubicTo` (exists) or `PathBuilder.curveTo` (the correct one). The `baseline-prof.txt` file in REFERENCES is the ground truth for method signatures on the exact Compose version used by the project. (source: D-242-fix14-ci, 2026-08-22)
- [PATTERN] The sandbox reaps the ENTIRE process tree of every Bash tool call when the call ends — even `setsid nohup ... &` dies. Long-running processes (the Android emulator) survive ONLY with a DOUBLE fork: `( setsid nohup CMD > log 2>&1 < /dev/null & )` — the subshell exits immediately, the command reparents to init. Single-fork sometimes appears to work; it doesn't survive reliably. (source: D-246 emulator session, 2026-08-23)
- [PATTERN] adb in this sandbox hangs tool calls unless EVERY invocation is wrapped: `timeout -s KILL <N> adb ... < /dev/null`. Two requirements: SIGKILL (default SIGTERM doesn't reliably kill adb clients) + stdin from /dev/null (adb inherits the persistent shell's never-EOF stdin). If `adb devices` hangs: `kill -9 $(pgrep -f fork-server)` then `adb start-server`. (source: D-246 emulator session, 2026-08-23)
- [PATTERN] `adb shell input text` silently truncates at ~14 chars. Type in ≤12-char chunks with pauses and VERIFY via UI dump — or skip typing entirely and inject via SharedPreferences files (adb push to /data/local/tmp + `run-as <pkg> cp`). (source: D-246 emulator session, 2026-08-23)
- [INSIGHT] Android emulator on an x86_64 host FATALLY refuses ARM system images ("Avd's CPU Architecture 'arm64' is not supported by the QEMU2 emulator on x86_64 host") — no flag bypasses it. Use an x86_64 image + a native x86_64 APK (CI test artifact), or the google_apis x86_64 image's libndk_translation for arm APKs (at ~3x memory cost). (source: D-246 emulator session, 2026-08-23)
- [INSIGHT] The sandbox has a hard 4GB memory cgroup — qemu (TCG) RSS ≈ guest RAM × ~3, so 1024MB guest RAM is the ceiling; 1536MB gets the emulator OOM-killed mid-session even with host RAM apparently free. Kill the idle next-server (~500MB RSS) before booting the emulator. Emulator death signature: pgrep qemu-system → 0 + dmesg "Out of memory: Killed process ... qemu-system-x86". (source: D-246 emulator session, 2026-08-23)
- [PATTERN] System ANR dialogs ("Process system isn't responding" / SystemUI) are NORMAL under 2-core TCG emulation during app cold starts — they are emulator artifacts, NOT app bugs. Always check WHICH process the ANR names before blaming the app. Dismiss with the Wait button; suppress background ones with `settings put global anr_show_background 0`. (source: D-246 emulator session, 2026-08-23)
- [PATTERN] A "pause on network loss" feature is INCOMPLETE without a visible resume path: (a) the restart logic (tryStartNext) only sees QUEUED/RETRYING tasks — paused tasks are invisible, so pausing without remembering+re-queuing means nothing ever resumes; (b) a foreground service that stops itself when the queue "empties" kills the NetworkCallback that would detect connectivity returning. Pause must pair with: a remembered set + explicit re-queue on network-regain + the listener service staying alive while paused tasks exist. (source: D-246, 2026-08-23, user-reported "no auto restart after internet is live again")
- [PATTERN] A cache keyed by identity derived ONLY from in-memory state (ResolvedVideosRegistry) silently BYPASSES the cache in every new app session (the registry dies with the process). Persist the identity, or recover it from the cache itself — conservatively (single unambiguous prior entry + matching discriminator) so a wrong-identity reuse can never file one video's bytes under another entry. (source: D-246, 2026-08-23, user-reported "cached videos still load from the network")
- [INSIGHT] OkHttp `response.use { ... return ... }` with a non-local return CLOSES the response (and its body stream) when the function returns — deadly when returning a streaming Response whose body NanoHTTPD's worker reads later: the client gets a dead stream. Rule: never `use{}` a response whose body outlives the block; close manually on non-streaming exits and let the wrapping InputStream own the close. (source: CR-C review, video-caching session 2, 2026-08-23 — compiler-verified with a runtime probe against real okhttp 5.0.0-alpha.14)
- [INSIGHT] "Downloaded bytes > total bytes" in a progress UI is almost always a STALE ESTIMATE, not a data bug: the total came from a running average (HLS) or a persisted prior run, and the downloader legitimately passed it. Fix at the display layer: when downloaded exceeds the reported total, grow the total to reality — and clear persisted tracker state on retry/restart. (source: D-246, 2026-08-23, user-reported size-inversion bug)
- [PATTERN] Cooperative cancellation (`coroutineContext.ensureActive()`) can't interrupt a thread blocked in a socket read — a paused download worker waits out the full read timeout (60s) before noticing. For instant pause/cancel: register every in-flight OkHttp Call in a registry and `call.cancel()` them from `job.invokeOnCompletion { if (cancelled) registry.cancelAll() }`. (source: D-246, 2026-08-23)
- [INSIGHT] Two sandbox-side compile-review techniques that keep catching real errors without local builds: (1) download the REAL dependency jars from Maven Central and compile the actual changed files with standalone kotlinc (matches the project's Kotlin version); (2) runtime probes for behavior questions (e.g. does response.use{} close the body? does Semaphore over-release throw?). Caught 9+ pre-push bugs across CR-A..CR-D this way. (source: D-243..D-246 sessions, 2026-08-22/23)
- [PATTERN] A cache bounded only by "what the player requests" is UNBOUNDED — media players (MPV/ffmpeg) read-ahead aggressively (demuxer buffers can be 128-256MB; HLS demuxers prefetch segments far past the playhead; under slow decode the whole file streams before the first frame). Bound caching by the PLAYBACK POSITION (a time window around the playhead), never by request patterns: (a) cap tee writes at pos+ahead; (b) for segmented formats, gate per-segment caching on the segment's time-range vs the window; (c) keep a byte-budget fallback for the pre-playback window-unknown phase — that fallback gap let 43MB cache before FILE_LOADED under slow decode. (source: D-247, 2026-08-23, user-reported whole-episode caching, emulator-verified)
- [INSIGHT] Byte↔time mapping for cache windows: HLS gives EXACT mapping for free (EXTINF durations → cumulative segment start-times, parsed during playlist rewrite). Progressive files only get a proportional (CBR) approximation — good enough for windowing (worst case the window is offset by seconds); don't over-engineer a time-index for progressive. (source: D-247, 2026-08-23)
- [PATTERN] "The episode must never fail because of the cache" needs an explicit retry-path policy: the proxy's in-request fail-opens (redirect/close) cover transport errors, but CORRUPT CACHED BYTES surface as player decode errors AFTER a 200 — the only recovery is at the player's error handler: first retry bypasses the cache (direct URL), subsequent retries too until a successful load re-arms it. (source: D-247, 2026-08-23)
- [INSIGHT] PlayerStateHolder's position/duration flows are Int (seconds) while cache-window math wants Float — no implicit widening in Kotlin argument positions. The standalone-jar compile probe catches these; always probe new public APIs with wrong-typed calls to verify the signature contract. (source: CR-E, D-247, 2026-08-23)
- [PATTERN] A null check on a data-class property from ANOTHER Gradle module does NOT smart-cast at the use site ("Smart cast to 'X' is impossible — public API property declared in different module"). Always capture cross-module nullable properties into a local val before the null-checked use. Compile probes must cover EVERY new branch, not just the extracted helper — the D-248 CI failure was in a fallback branch the probe skipped. (source: D-248 CI fix 4f367a81, 2026-08-23)
- [PATTERN] Statistics derived from an activity log must filter for MEANINGFUL events, not raw rows: bulk operations (AniList sync mark-all, manual marks) write hundreds of rows stamped with the same last_watched_at + duration=0, and any per-day row-count stat explodes ("2,333 episodes in one day"). Filter on a watch-fraction threshold (≥10%) + a manual-vs-organic flag — duration=0 rows fail the fraction test automatically. (source: D-248, 2026-08-23)
- [PATTERN] Partial-axis UPDATE queries (updateDataSourceAxis/updateExtensionAxis) overwrite EVERY column of the axis with the passed values — nulls propagate. Never construct a bare details object for such an update: fetch the existing row and .copy() only the fields that changed, else you wipe data you didn't mean to touch (the D-248 cover-wiping bug did exactly this on every app start). (source: D-248, 2026-08-23)
- [INSIGHT] A reconciliation scan that shrinks a durable manifest to match a transient observation is a data-loss machine: the walk glitched (SAF latency), not the files. Rule: NEVER let a scan write a SMALLER durable list unless every dropped entry is independently confirmed gone; treat rebuilt < existing as a transient failure + protect the existing entries from cleanup. (source: D-248 anti-shrink guard, 2026-08-23)
- [PATTERN] When `deploy-dashboard.yml` (2-job build→deploy split) is triggered via `workflow_dispatch` on a NON-default branch and the `deploy` job fails with `BlobNotFound` (Azure blob error, ~20s in), the root cause is NOT the 2-job split — it's the `github-pages` environment's `branch_policy` protection rule (only allowlisted branches can deploy). The `build` job succeeds (artifact uploaded) but `actions/deploy-pages@v4` can't retrieve it because the environment rejects the branch. FIX: add the branch to the env's deployment-branch-policies via the GitHub API (`POST /repos/{o}/{r}/environments/github-pages/deployment-branch-policies` with `{"name":"<branch>"}`) — a repo SETTING, not a workflow file change — then rerun the failed deploy job. Do NOT rewrite the workflow into a single job to "fix" this; the branch_policy is the real gate. Applies to ANY non-default-branch dashboard deploy (feature branches, test branches). (source: 2026-08-24 review session, deploy run 32731668048)
- [PATTERN] `deploy-dashboard.yml` auto-triggers on `push` to `main` ONLY (line 13-14). To deploy the dashboard from a feature branch WITHOUT modifying the workflow file, use `workflow_dispatch` (the workflow already supports it, line 18) via the GitHub API (`POST /repos/{o}/{r}/actions/workflows/deploy-dashboard.yml/dispatches` with `{"ref":"<branch>"}`). This honors "nothing else changes" when the user wants a branch's dashboard deployed temporarily. Combine with the branch_policy fix above. (source: 2026-08-24 review session)

- [PATTERN] When the user says one screen's icons look like "proper SVG" but another screen's icons look like "some other kind of format," the root cause is almost always the ICON CONTAINER treatment, not the glyph: both screens likely use the same `Icons.Filled.*` Material vector, but one renders it bare (24dp `Icon` tinted `primary`, no `Surface`) and the other wraps it in a `primaryContainer` rounded box ("chip-box" / "tile"). Fix = unify on the bare-icon style (reuse the proven `MoreListRow` everywhere); do NOT introduce a new shared `*NavRow` that duplicates the bare-icon row (§5 — two composables doing the same thing is an unrequested abstraction). Audit via `grep -rn "primaryContainer" <settings-dir>/` — any hit inside an `Icon`'s wrapping `Surface` is the chip-box pattern to remove. (source: D-250, 2026-08-24, user-reported More-vs-Settings icon inconsistency)
- [PATTERN] A small composable duplicated 10+ times across files (`BackAction` — a 36dp circular back button) is a dedup magnet: each copy drifts independently (2 of 12 lost the `Modifier.size(18.dp)` so the icon rendered at 24dp; 1 used `clip(CircleShape)` + `onBackground` tint + 20dp instead of `background` + `onSurfaceVariant` + 18dp). Promote to `:core:designsystem` ONCE; replace all call sites; the drifts auto-fix. Rule of thumb: if `grep -rn "private fun <Name>("` returns >3 hits across files, it belongs in the shared designsystem module (or a shared package-private helper if it can't live in designsystem). (source: D-250, 2026-08-24, 12 BackAction duplicates)
- [INSIGHT] Kotlin does NOT fail compilation on unused imports — and the ANI-KUTA project has NO `ktlint`/`detekt`/`spotless`/`.editorconfig` (verified 2026-08-24). So a refactor that deletes a local composable (leaving its formerly-needed imports unused) will still pass CI. This is a SAFETY NET for mechanical dedup refactors (delete the def + add the shared import; the leftover imports are warnings, not errors). BUT it's technical debt — queue a tidy commit. Don't confuse "CI green" with "clean" here. (source: D-250, 2026-08-24, sub-agent flagged ~30 dead imports as ⚠️ but verified CI-safe)
- [MISTAKE] When swapping a `*NavRow` call site that was nested INSIDE a `SettingsGroupCard` content lambda to a shared row composable (`MoreListRow`) that has its own baked-in 16dp horizontal padding, the row would double-pad inside the card (card's 16dp + row's 16dp = 32dp left inset). Fix = move the nav row OUT of the card as a standalone sibling (keep any `AnimatedVisibility` wrapper), so the row's padding is the only inset. Don't try to "override" the baked-in padding via the `modifier` param — it's applied AFTER the passed-in modifier in the component's internal chain. (source: D-250, 2026-08-24, NotificationsSettingsScreen LibraryNavRow-in-card move)
- [PATTERN] GitHub's `/releases/latest` endpoint EXCLUDES prereleases AND drafts — every release flagged `prerelease:true` is invisible to it. If an in-app updater must see all releases, query `/releases?per_page=N` (includes prereleases, excludes drafts) and pick the best by version yourself. Symptom seen: app at 0.2.x reported "up to date" while 11 newer prereleases existed; `/releases/latest` returned v0.2.6 from weeks earlier. (source: D-251 update-checker fix, 2026-08-24)
- [PATTERN] Packed version codes (`major*10000 + minor*100 + patch`) COLLIDE at patch ≥ 100 ("0.2.100" == "0.3.0" both → 300). Compare versions as integer tuples/lists lexicographically instead; if a single Long is needed for a data class, use wide multipliers (major*1_000_000 + minor*10_000 + patch) and treat it as informational only. (source: D-251 GitHubUpdateSource rewrite, 2026-08-24)
- [PATTERN] Android-emulator on a NON-ext4 rootfs (overlayfs containers): emulator 35+/37+ disables QuickbootFileBacked and runs a worst-case userdata pre-check ("Not enough space to create userdata partition … need ~2× image + full data partition") that FATALs even though actual usage is ~350MB (qcow2 overlays + sparse userdata). Bypass WITHOUT root: compile a tiny LD_PRELOAD statvfs/statfs shim that inflates f_bfree/f_bavail for `.avd` paths only (`gcc -shared -fPIC freedom.so`). Do NOT pre-create `userdata-qemu.img` as a zero file to skip the check — the emulator then skips FORMATTING and the guest dies at "Error creating cache dir /data/dalvik-cache". (source: D-251 sandbox emulator rebuild, 2026-08-24)
- [PATTERN] New emulators force-bump x86_64 guest RAM to 2048MB ("Increasing RAM size to 2048MB" — fatal under a 4GB cgroup with TCG). Bypass: append `-qemu -m 1024` AFTER the emulator's own `-memory` flag — qemu takes the LAST `-m`, so the guest gets 1024MB while the emulator's internal accounting stays 2048 (harmless with -no-snapshot). Also required on this container: `-accel off` (no KVM → hard-refusal otherwise) + `ANDROID_SDK_ROOT` exported + a `platforms/android-30` dir present. Verified: RSS ~1.8GB peak, cold boot ≈9 min. (source: D-251 sandbox emulator rebuild, 2026-08-24)

- [MISTAKE] `staticCompositionLocalOf` is a top-level FUNCTION in androidx.compose.runtime, not a type — `val LocalX: staticCompositionLocalOf<T> = staticCompositionLocalOf(...)` does NOT compile (unresolved reference in type position). The idiomatic pattern omits the type annotation entirely: `val LocalX = staticCompositionLocalOf { initialValue }` (inferred `StaticCompositionLocal<T>`). Caught by the Task-7 compile review after I wrote the annotated form in CustomTheme.kt:76. (source: D-254, 2026-08-25)
- [PATTERN] M3 `Surface` applies its OWN shape-clip AFTER any user-supplied modifier — so `Surface(shape = S, modifier = Modifier.drawBehind { ... })` paints the drawBehind content OUTSIDE the shape's clip (it spills past S). When custom-drawn background geometry must be trimmed to a shape, put the clip in the modifier chain BEFORE the draw: `modifier = Modifier.clip(S).drawBehind { ... }`. Caught by the 6-r plan review for the D-252 compound badge's pointed tip. (source: D-252, 2026-08-25)

- [MISTAKE] NEVER wrap long-lived stateful content (an app root!) in a conditional that flips on a settings change: `if (x != null) { Provider { Content } } else { Content }` MOVES `Content` between composition call sites when x flips — destroying every `remember{}` under it (nav backstacks, scroll states, view models' UI state). The user saw "selecting a palette navigates me to Browse". Rule: keep ONE stable structure and switch VALUES — `Provider(Local provides (x ?: sentinel))` — never switch the wrapping structure. (source: D-255, 2026-08-25, AnikutaTheme if/else)
- [PATTERN] A BOM (`platform()`) is a PREFER-constraint — any TRANSITIVE dependency with a higher version silently wins at app resolution. koin-compose 4.2.2 (→ org.jetbrains.compose.foundation:1.10.2 → androidx aliases) silently bumped the app's RUNTIME compose stack to 1.10.4 while modules compiled against the BOM's 1.7.8 → NoSuchMethodError on APIs whose signatures changed (FlowRow). Detection trick that worked: download the CI APK artifact and read `META-INF/androidx.versions` + grep the dex string pool for method prototypes (runtime signature had an extra `Alignment$Vertical` param vs the crash's expected one) + diff foundation-layout sources jars (1.7.8 vs 1.8.0) to pin the exact change. Diagnosis without a single local build (§8). (source: D-255, 2026-08-25, custom-palette crash)
- [INSIGHT] Per-MODULE compile classpaths resolve independently: modules that depend on the version-pulling library (koin-compose) compile against the NEW version and work at runtime; modules without it compile against the BOM version and crash on changed APIs. So a version-skew crash is MODULE-SCOPED, not app-scoped — "module X works but module Y crashes on the same API" is the tell-tale signature. (source: D-255, 2026-08-25, designsystem+player vs search/details/watch)
- [MISTAKE] `MatchResult.Destructured` provides component1()..component5() ONLY — destructuring SIX capture groups (`val (y,mo,d,h,mi,s) = match.destructured`) does not compile. Use `match.groupValues[1..6]` instead. Caught by the Task-10 compile review. (source: D-255, 2026-08-25, parseIsoDate)
- [MISTAKE] `java.time.*` needs coreLibraryDesugaring below API 26 — and the resulting `NoClassDefFoundError` is an `Error`, NOT caught by `catch (Exception)`, so a "defensive" try/catch around java.time parsing does NOT protect minSdk-24 devices. The old comment "API 26+ is our minSdk" was wrong (minSdk = 24). For fixed-format ISO dates, regex + `java.util.Calendar` (UTC) is the safe parse on every API level. (source: D-255, 2026-08-25, GitHubUpdateSource)
- [PATTERN] Wraparound auto-advance carousels: `animateScrollToPage((current + 1) % size)` animates BACKWARDS through every page on the last→first wrap (a fast reverse sweep that reads as a glitch). The fix is a VIRTUAL infinite pager — `pageCount = size × N` (N≈200), `initialPage = size × N/2`, display index = `page % size`, advance always +1 — wrapped in `key(items.size)` so a list-size change recreates the state at display index 0. Forward-only steps are always one smooth tween. (source: D-257 hero v3, 2026-08-25, "auto-scrolling is not smooth, not animated")
- [PATTERN] Coil 3 preload recipe (verified from the 3.0.4 sources): the ANDROID memory-cache key EXCLUDES size unless the request has transformations, and compose's AsyncImage forces INEXACT precision when unset — so `imageLoader.enqueue(ImageRequest.Builder(ctx).data(url).size(wPx, hPx).build())` at the composable's exact pixel dims is a memory-cache HIT for the later AsyncImage (INEXACT accepts cached ≥ requested). Gotcha: `ImageRequest` lives at `coil3.request.ImageRequest`, NOT the coil3 root package — the root-package import compiles nowhere (CI run 32845772374). (source: D-257 SectionPreloader + its CI fix, 2026-08-25)
- [PATTERN] Activity-scoped ViewModels (custom nav backstacks without per-entry ViewModelStoreOwner) make "load defaults in init" a ONCE-PER-PROCESS guarantee: after any state reset (clear button, error path), nothing ever reloads. Fix pattern: a single idempotent `loadDefaults()` (in-flight Job guard + already-showing flag) funneled from EVERY "query/state became blank" path (clear handler, text-change blank branch, debounced collector, init, screen-resume) + staleness guards at every async completion (re-check the trigger condition before writing state) so late responses can't clobber newer state. (source: D-258 search default-results bug, 2026-08-25)
- [CORRECTION] User instruction (2026-08-25): if the Bash tool fails MORE THAN 10 times in a session — total, not just consecutive — STOP all work and SEND AN NTFY NOTIFICATION; do not keep hammering. Previously §27 only capped 5 CONSECUTIVE same-tool failures with no cumulative limit and no notification, so intermittent failures could rack up indefinitely without the user knowing. Codified as the HARD LIMIT in CORE_RULES §27 rule 4 (count resets on a new user message).
- [PATTERN] `Color.value.toInt()` returns 0 for every sRGB color. Compose's `Color` is a `value class Color(val value: ULong)` with ARGB packed in the UPPER 32 bits; `.toInt()` truncates to the LOWER 32 bits = `0x00000000` (fully transparent). When packing a Compose `Color` to an ARGB `Int` for persistence (SharedPreferences, etc.), use `.toArgb()` (`androidx.compose.ui.graphics.toArgb`). The bug manifests as "transparent theme on restart" + "picker opens at #00000000" — silent because the writes succeed (no exception), they just store 0. (source: D-261, two palette bugs root-caused as one)
- [INSIGHT] Coil 3 (3.0.x) REMOVED the `Transformation` API entirely — there is NO `coil3.transform.Transformation` (verified from the coil-core 3.0.4 sources jar: no `transform/` package). Coil 2 had it; Coil 3 dropped it. Apply per-request image effects via: (a) a custom `memoryCacheKey`-namespaced small-decode + CPU work on `Dispatchers.IO` + render via `Image(BitmapPainter, ContentScale.Crop)` for the GPU upscale (works on every API), OR (b) `Modifier.blur(radius)` which is API 31+ only (RenderEffect; silent no-op below). `Modifier.blur` is fine when you ONLY target 31+; for minSdk 24 use the decode+CPU approach. Also: `coil3.request.ImageResult` is a sealed interface; its success subclass is the TOP-LEVEL `coil3.request.SuccessResult` (a `@Poko` class), NOT `ImageResult.Success` (not nested). `coil3.Image.toBitmap(width, height)` is an `expect` in `coil3/Image.kt` — `import coil3.toBitmap` to use it. (source: D-262 hero blur research + its 2 CI fix rounds)
- [MISTAKE] `LaunchedEffect(pagerState.currentPage, ...)` keyed on `currentPage` cancels mid-`animateScrollToPage` — `currentPage` flips at the 50% scroll crossing during the animation, which changes the LaunchedEffect's key → the effect coroutine is cancelled mid-flight → the pager rests at offset ≈ 0.5 with NO snap (HorizontalPager only snap-flings on user-gesture end, not on cancelled programmatic scrolls). The effect was also single-shot → once cancelled it never re-armed → auto-advance died permanently. Fix: key on `(pagerState, virtualCount)` — NEVER on a page index — and use a `while(true)` loop + `try { animateScrollToPage } catch (CancellationException) { if (!currentCoroutineContext().isActive) throw e; snapshotFlow { isScrollInProgress }.first { !it }; withContext(NonCancellable) { if (currentPageOffsetFraction != 0f) scrollToPage(currentPage) } }`. Dots should read `settledPage` (update on settle, not mid-slide). (source: D-262 hero "stuck between two banners")
- [PATTERN] Coil 3 returns `Bitmap.Config.HARDWARE` by default on API 26+ — and `Bitmap.getPixels()` on a hardware bitmap throws `IllegalStateException` (hardware bitmaps are GPU-resident, not CPU-readable). If you read pixels from a Coil-loaded bitmap (for blur / Palette / getPixels / any CPU work), ALWAYS set `.allowHardware(false)` on the ImageRequest (import `coil3.request.allowHardware`) AND defensively copy (`if (src.config == Bitmap.Config.HARDWARE) src.copy(ARGB_8888, true)`) before getPixels. The crash is silently swallowed if wrapped in `try/catch (_: Exception)` — the bitmap work becomes a silent no-op + the UI shows a blank placeholder. (source: D-266 hero banner "does not show at all", 2026-08-26)
- [MISTAKE] Launching `refreshTracking()` (or any function that reads VM state set by a concurrent load) in a SEPARATE `viewModelScope.launch` races the main load — the state it reads (currentMainId / anilistId / state.value) is null/loading at launch time → early-returns → never re-invoked after the load completes. Fix: call the refresh AT THE END of the load path that establishes the prereqs (e.g. inside `mergeAniListIntoUnified` after `linkAniList` + `remergeBases` set currentMainId + anilistId + state=Success), guarded by `if (loadGeneration == gen)`. Don't fire-and-forget a refresh that depends on state the concurrent load hasn't set yet. (source: D-270 search→extension→detail tracking "does not properly reload", 2026-08-26)
- [PATTERN] Direct `LazyGridState`/`LazyListState` reads (`firstVisibleItemIndex`, `firstVisibleItemScrollOffset`) in a parent `@Composable` body cause per-frame parent recomposition (the state mutates every scroll frame → the parent recomposes → re-allocates lambdas passed to children → children can't skip → per-item anti-patterns re-run → compounds on fling). Wrap in `remember(keys) { derivedStateOf { ... } }` — the parent only recomposes when the DERIVED value flips (e.g. `collapsed` true↔false), not on every frame. Use `remember(keys)` (not bare `remember`) when the derived lambda captures a non-state val that changes on recomposition (e.g. `isList` from `displayMode`). (source: D-269 library scroll jitter, 2026-08-26)
- [PATTERN] A fixed `aspectRatio(16:9)` + `ContentScale.Crop` on natively-WIDER artwork (AniList banners are ~3:1 to ~3.57:1) silently discards ~40-50% of the image — the user sees only the center horizontal band and reports "the banner is cropped / not shown properly / forced into a square vibe". To show the FULL source image: `ContentScale.Fit` + an explicit `Alignment` (e.g. TopCenter for "banner at top"), and size the container for banner + content zone (a 1.2:1 card fits a ~3:1 banner at the top ~40% + a content zone below). Fit leaves letterbox space wherever the ratios mismatch — give the container a palette-derived base color so the letterbox reads as intentional design, not empty gap. (source: D-277, the 4th hero iteration — D-257/D-262/D-275 all kept Crop on a fixed ratio)
- [INSIGHT] `:core:designsystem` has NO Koin dependency (deliberately framework-light). A new file there using `org.koin.compose.koinInject` compiles NOWHERE — the import itself is an unresolved reference. Before using ANY framework symbol in a module you haven't touched before, grep that module's `build.gradle.kts` for the dependency (10 seconds) instead of discovering it on CI. To get a Coil `ImageLoader` in a Koin-less module: `LocalContext.current.imageLoader` (coil3 extension) — the app's singleton with the 500MB OkHttp disk cache. (source: D-277, caught pre-commit during self-review)
- [PATTERN] For "screen must show default content offline", cache-FIRST beats catch-fallback: an OkHttp connect timeout is up to 30s, so a catch-only fallback shows Loading for half a minute before content appears. Serve the cache row instantly (DB read + JSON decode ≈ ms), then refresh from network in the same coroutine; on network failure KEEP the served cache (only fall to empty/Idle when no cache row exists). Reuse an existing cache table when the query is identical — Search's trending default is the EXACT same AniList TRENDING query Browse caches, so a shared codec over `browse_cache` gives offline defaults with zero new storage. (source: D-278)
