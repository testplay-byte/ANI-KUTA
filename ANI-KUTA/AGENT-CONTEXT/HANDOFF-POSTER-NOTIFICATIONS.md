# HANDOFF — the poster-notification + preview issues (rounds 46-47, v1.1.9/v1.1.10 device findings)

> Written by the round-46/47 agent for the NEXT agent. Read this alongside
> `SESSION.md` + `CORE_RULES.md`. The user tested v1.1.10 on device and the
> features below DO NOT work yet. This file is the full technical picture:
> what was built, what breaks, root-cause hypotheses, and where to dig.

## 1. What exists (built in rounds 46-47, all on `feature/ads-return-pill`)

| Piece | File(s) | State |
|---|---|---|
| Poster art provider seam | `core/notifications/.../NotificationArtProvider.kt` | interface, fine |
| NotificationManager poster path | `core/notifications/.../NotificationManager.kt` — `postNotification` (BigPictureStyle when `artProvider` returns a bitmap; BigText fallback), `postPosterNotification` (the test/preview primitive), small icon = `R.drawable.ic_notification` | compiles; the FALLBACK is what fires on device |
| Poster config keys | `core/preferences/.../NotificationPreferences.kt` — `posterEnabled`, `posterBackgroundSource` ("banner"/"cover"), `posterShowEpisodeTitle`, `posterShowEpisodeThumbnail`, `posterShowAudioBadge`, `posterShowBranding` | fine |
| The composer | `app/.../notifications/EpisodeBannerComposer.kt` — 1024×576 Canvas composition (art center-crop + scrim + title + "EPISODE N" lime + episode title + SUB/DUB chip + episode thumbnail chip + branding) | compiles; **returns null on device** (the exception is logged — see §2) |
| Demo picker | `app/.../notifications/EpisodeDemoPicker.kt` — random library content WITH cached episodes + its latest episode | untested on device |
| Test notifications | `app/.../notifications/EpisodeNotificationTester.kt` (feed-first → library fallback) + `DelayedPosterTestWorker.kt` (5-min staggered second post) | posts arrive but WITHOUT banners on device (the same composer failure) |
| Preview page | `app/.../settings/NotificationPosterSettingsScreen.kt` — live preview (feed-first → library fallback → "No episodes available yet" state) + composition toggles + shuffle | **shows "Couldn't load the preview art" on device** — the composer threw; the exception text is swallowed into a log line |
| Notification icon | `core/notifications/src/main/res/drawable/ic_notification.xml` (kawaii mouth, monochrome) | works |
| Updates-apply fix | `core/updates/UpdateStore.observeUnacknowledgedCountForMain` + `feature/anime-details/impl/.../DetailsViewModel.kt` D-475 observer (auto `refreshEpisodesListNow()` when unacknowledged rows rise) | untested on device — verify with a real update detection |
| Settings IA | `app/.../settings/SettingsScreen.kt` (one "Updates & Notifications" row) + `NotificationsSettingsScreen.kt` (Sub/Dub/Both SegmentedToggle + poster nav row + the new test row) | the user wants the Sub/Dub/Both block RESTRUCTURED (see §4) |
| Library badges | `feature/anime-library/impl/.../LibraryScreen.kt` `CoverBadgeRow` (soft pills, `BadgePosition.TOP_CENTER` default in `LibraryViewModel.kt`) | user approved the style, then asked to CENTER AT TOP (done in v1.1.10) — re-verify on device |

## 2. THE two on-device failures (v1.1.10) — diagnose before changing anything

### 2a. The preview shows "Couldn't load the preview art"
`failed = true` means `EpisodeBannerComposer.buildBanner` THREW (the only
paths returning null are the catch blocks — compose() itself always returns
a bitmap). The exception is logged via `Logger.e/w(TAG…)` with tag
`Anikuta:App:BannerComposer`. **First step: reproduce + capture logcat** (the
user tests via Android Studio's Logcat panel — CORE_RULES §20 filter format):

```
tag:Anikuta:App:BannerComposer | tag:Anikuta:App:NotifTester | tag:Anikuta:Core:Notifications
```

Candidate root causes to check, in order of likelihood:
1. **Main-thread DB/IO inside `produceState`**: the preview's `produceState`
   coroutine runs on the MAIN dispatcher and calls `updateStore.getAllUpdates`,
   `contentRepository.getMainEntryByMainId`, `dataCacheRepository.getEpisodeMetadata`
   (blocking `executeAsList`) + Coil network loads. If StrictMode or the DB
   lock bites, it throws. FIX DIRECTION: move the whole composition into
   `withContext(Dispatchers.IO)` inside the composer, or wrap the preview's
   produceState body in `withContext(Dispatchers.IO)`.
2. **Coil `imageLoader` access**: `context.imageLoader` (coil3) inside the
   composer resolves the app's singleton — verify this is non-null from an
   application-context-only path (the proven pattern in
   `UpdateProgressNotifierImpl.loadCoverAsync` uses the SAME access from a
   worker; compare and mirror it exactly).
3. **`DataSourceExtras.fromJson`** on a null/malformed `dataExtraJson` — the
   parser is `ignoreUnknownKeys` + runCatching-wrapped upstream, unlikely.
4. The `maxBy { it.episodeNumber }` on an empty list is guarded (skip), but
   `episodes.maxBy` vs the Float `episodeNumber` — check nullability.

### 2b. The test notifications post as PLAIN TEXT (no banner)
Same composer exception → the BigText fallback fires. Fixing 2a fixes this.
Additionally the user asked WHY the network is needed at all: the art comes
from remote URLs. Improvement direction: prefer Coil's disk cache (500 MB,
already configured in `AnikutaApp`'s ImageLoaderFactory) and consider
persisting the composed banner (filesDir) per (mainId, episode) so
re-tests are instant and offline.

### 2c. The user's explicit product asks for the NEXT agent
1. **The preview must work offline-first** — locally available art (Coil disk
   cache / any locally stored cover) before touching the network.
2. **The preview + test posts must pick**: feed row first; else a RANDOM
   library content with cached episodes (latest episode), re-rolled per open
   (`EpisodeDemoPicker.pickRandom` — verify it returns content on the user's
   7-anime library) + the "Shuffle preview" action re-rolls; never unlinked/
   episode-less content; the "No episodes available yet" state otherwise.
3. **The test notifications**: the first immediately, the SECOND 5 minutes
   later (DelayedPosterTestWorker — verify the 5-min delay actually fires;
   the user saw both arrive simultaneously in v1.1.9 — in v1.1.10 the
   staggered worker exists but was untested on device).
4. **The episode-type (Sub/Dub/Both) block on the Notifications screen must
   look EXACTLY like the Updates screen's**: title + description at the top,
   the full-width SegmentedToggle BELOW (not in a row's trailing slot).
   Mirror `UpdatesSettingsScreen.kt` L293-323's structure.
5. **The library badges**: the user approved the soft-pill restyle and asked
   to CENTER AT TOP (TOP_CENTER shipped) — verify on device, then polish
   further per fresh feedback (they said "not handled properly as I hoped"
   about v1.1.9's state, then approved the pills in v1.1.10, then asked for
   centering — get a screenshot-level confirmation this time).

## 3. The update-apply fix (D-475) — verify, don't assume
`DetailsViewModel`'s D-475 observer auto-refreshes when unacknowledged
episode_update rows rise for the open anime. It has NEVER been device-tested.
Verify: add library anime → force a check (Updates settings → "Check for
updates now") while the details page is open → the episode list should grow
by itself. Watch for: the observer loop (own writes are pre-acknowledged, so
it shouldn't), and the refresh not running while `_isRefreshing` is true.

## 4. CI state (D-472) — do not regress
build-apk.yml no longer triggers on `release/**` or docs-only pushes; at most
2 runs per release (1 feature push + 1 tag run). The unit-test steps are gone
(D-465) unless the user asks. The What's New release body = the annotated
tag's body (D-466) — ALWAYS write user-facing bullets in the tag annotation,
never commit logs.

## 5. Branch/version state at handoff
- `feature/ads-return-pill` = the full v1.1.4..v1.1.10 line (CI green).
- `release/1.1.10` + tag `v1.1.10` = the current LIVE debug release
  (1.1.10/10110, ani-kuta-v1.1.10-debug-arm64-v8a.apk).
- `main` has NONE of this — the user has NOT confirmed the merge.
- The version bump lives ONLY on the release branches (D-425 discipline).
- Release notes = the tag annotation body (D-466) — the workflow turns them
  into the GitHub release body the app displays.

## 6. RESOLVED (round 49, D-491) — the §2a root-cause hypotheses were NOT the cause
The v1.1.11 device logcat (the user's round) delivered the definitive answer:
`IllegalArgumentException: Software rendering doesn't support hardware
bitmaps` at `EpisodeBannerComposer.drawCenterCrop` ← compose ← buildBanner.
Coil decodes into `Bitmap.Config.HARDWARE` by default on API 26+; a HARDWARE
bitmap cannot be drawn on the composer's SOFTWARE canvas
(`Bitmap.createBitmap`) — the first draw threw, the catch nulled the banner,
and the UI blamed the connection. This doc's §2a hypotheses (main-thread
blocking DB reads) were real-but-orthogonal: D-486 fixed them, and the
device STILL failed — the bitmap config was the missing layer. The fix:
`bitmapConfig(ARGB_8888)` at the loadBitmap choke point (+ the engine's
`isCacheValueValidForHardware` memory-cache validation) + `ensureSoftwareSafe()`
copy-or-null as the never-crash last line. The "toBitmap converts hardware
bitmaps" comment that justified the old code was false — verified against
the coil3 3.0.4 bytecode (BitmapImage is returned as-is). Lesson for the
next agent: a catch-only-null failure mode HIDES the real exception class —
round 48's exception-class-in-log discipline is what finally cracked this.


## 7. ROUND 50 (D-493..D-496) — the presentation round: what changed after the pipeline went green
The v1.1.12 device round approved the PIPELINE (posters render end-to-end) and flagged the PRESENTATION. What the next agent must know:
- **The canvas is 1024×440 (≈21:9)** — `EpisodeBannerComposer.CANVAS_WIDTH/HEIGHT` (PUBLIC consts; the companion is public on purpose — Kotlin forbids cross-file access through a private companion's facade). The preview box reads them for its aspectRatio.
- **The layout is zone-anchored** (text from the top; the chip row + branding on a shared bottom baseline; the thumbnail aspect-preserved in a 210×330 box) — never re-introduce a flow layout that lets text push elements off-canvas.
- **Never use a BitmapShader without a local matrix for the cover-crop** — the v1.1.12 "glitched background" was a shader sampling at native size + CLAMP edge smear. The current `drawCoverFit` (src→dst rect math) is the keeper; background loads pass `coil3.size.Scale.FILL`.
- **The badge truth table**: the engine's `audioVariant` ∈ {"sub", "dub", "unknown"}; "unknown" means both-variants-or-unparsed. DEMO paths (preview feed-first, `normalizeForDemo`; the picker's random variant) normalize to sub/dub so the demo always shows a chip; the REAL path passes the engine value through untouched (unknown → no chip, honest — real posts only ever carry sub/dub anyway).
- **The preview's selection flow**: `selectPreviewContent` (file-level in NotificationPosterSettingsScreen.kt) — screen open: feed-first → library-random; SHUFFLE: library-random EXCLUDING the on-stage id (one-shot `shuffleExclude` ref, consumed at the top of the produceState pass); TOGGLE FLIPS: re-render the CACHED `PreviewSelection` (never re-select). The refs/cache are deliberately NOT produceState keys.
- **The tester spec** (EpisodeNotificationTester): the feed is NOT consulted; random distinct library entries (ANY entry qualifies); cycling when the library is small; `pickPureDemo()` when the library is EMPTY (blank mainId → the styled no-art stage; the title rides KEY_TITLE in the work data). The tester ALWAYS posts ≥1 when notifications are permitted.
- **Episode labels**: `episodeLabel` in NotificationManager's companion + the composer's `episodeLabel` — "EP 12"/"EP 12.5", never "EP 12.0".

## 8. ROUND 50 RELEASED (v1.1.13) + the release-first process (D-497/D-498)
- **v1.1.13 is LIVE**: release/1.1.13 = the feature head `24be155b` + ONE bump commit (`c66db803`, 1.1.13/10113 + the 42-RELEASE-1.1.13-BRANCH-POINT.md record); tag `v1.1.13`; Release APK run 35456726478 GREEN (the only CI run the release cost); stable, `--latest`, `ani-kuta-v1.1.13-debug-arm64-v8a.apk` + `SHA256SUMS.txt`. The user's device round happens ON this release (in-app update, 10113 > 10112).
- **D-498 THE STANDING PROCESS (the user's instruction — "Remember to handle the future changes exactly like this too")**: after an implementation push is CI-green, cut the release branch + tag IMMEDIATELY. The release IS the verification build; there is NO device round on a feature build before the tag and NO separate verification builds. A failed release run is diagnosed from the release CI logs; the ≤2-runs budget (D-472) = the implementation push + the release run.
- Both lines (feature + release) carry the final docs; docs-only pushes trigger no CI.

## 9. ROUND 51 (D-499..D-501) — banner v3, the dead seam, the in-app banner, the planned shuffle
The v1.1.13 round approved the pipeline and asked for presentation depth + two features. What the next agent must know:
- **The canvas is 1024×400 (2.56:1)**; the layout is MIRRORED: the episode thumbnail is a LEFT-side CARD (fixed 16:9 400×225 box, `drawThumbBox` cover-crops ANY source into it — the box's shape always wins), rounded + dark border + two-layer fake-blur shadow; the text column hangs top-RIGHT and the scrim is right-anchored. Never re-add a right-side thumbnail without re-mirroring the scrim.
- **The episode number is a TAG, not a hero line**: the tag row `[EP n] [SUB] [DUB]` sits under the ≤2-line title (EP chip = lime fill + dark label; audio chips = dark fill + lime label; "both" → both chips). All text carries the soft dark shadow layer (light-art safety is a user rule now).
- **The chip truth lives in the FEED**: `resolveAudioVariant` (composer) unions `UpdateStore.getVariantsForEpisode` (DISTINCT audio_variant for mainId+episode — the engine inserts SUB and DUB as separate rows) with the passed variant. Never trust a single row's variant alone; never let a DB failure fail the banner (degrade to the passed variant).
- **THE SEAM**: `single<NotificationArtProvider> { get<EpisodeBannerComposer>() }` in AnikutaApp is LOAD-BEARING — Koin indexes by concrete type, so without that explicit binding `getOrNull<NotificationArtProvider>()` in NotificationsModule resolves NULL and every system notification silently falls back to plain text (this shipped broken from D-477 until D-500 — the device never showed notification banners). Keep the binding; keep the same pattern for ANY new nullable interface seam.
- **The in-app banner**: InAppBannerController (core/notifications, replay=0 SharedFlow) ← NotificationManager emits after nm.notify() on BOTH poster paths (composed bitmap + texts) ← InAppBannerHost (AppRoot overlay stack, repeatOnLifecycle(STARTED) — foreground-only by construction; current/visible state split so the exit animation plays). Auto-dismiss 5s; tap dismiss.
- **The Shuffle is a DECK now**: EpisodeDemoPicker.ShuffleDeck (shuffled queue, refill + no-immediate-repeat head-swap, @Synchronized next, stale-id pruning); pickRandomPlanned(deck, exclude) with the D-494 no-op fallback; screen-open stays feed-first → random. The deck instance lives in the screen's remember{}.
- **v1.1.14 is LIVE** (D-502): release/1.1.14 = feature head `5141d1f1` + ONE bump commit (2f26f467, 1.1.14/10114 + the 43-RELEASE record); Release APK run 35462212024 GREEN; the user's device round happens ON this release.
- **Round 52 (D-503..D-506) — the clean-notification + Poster-Studio round**: the CLEAN banner rule (when a banner composes, BigPictureStyle sets ONLY bigPicture/bigLargeIcon and the builder title/text are "" — no text above the poster, both post paths; a failed compose keeps the old text card); the REAL heads-up popup (channel `anikuta_new_episodes_high` at IMPORTANCE_HIGH — the old DEFAULT channel never pops heads-up; legacy deleted in ensureChannel; PRIORITY_HIGH builders); the D-500 in-app overlay DELETED (InAppBannerController/InAppBannerHost gone — the user rejected the in-app card: "I wanted the banner to be shown as a notification"); the 30s test stagger (`index * 30L SECONDS` in EpisodeNotificationTester).
- **The chip truth is now THREE layers**: passed variant ∪ feed's per-variant rows (D-499) ∪ the EPISODE CACHE row parsed with `parseAudioAvailability(scanlator, sourceName ?: title)` — the same parser the details page's per-episode pills use. Both → both chips; one → one; nothing → no chip.
- **The scrims are GONE** — D-504 removed drawScrims entirely; the art renders full-brightness and the D-499 text shadows are the only readability carrier. Do not reintroduce a scrim without the user asking.
- **The thumb evidence chain**: episode still (12s, then one 8s retry) → extras.coverUrlLarge → cover → banner (6s each) — `loadThumbBitmap`. The left card survives null thumbnail_url rows; an empty chain still composes without the card.
- **THE POSTER STUDIO (D-505)**: `PosterLayoutConfig` (kotlinx-serialization JSON in `NotificationPreferences.posterLayoutJson` = `notif_poster_layout_json`) + `PosterCanvasMetrics` (the shared 1024×400 geometry) + `PosterDrawing` (the extracted draw primitives — composer AND studio render through the SAME code) + `EpisodeBannerComposer.loadEditorArt` + public `resolveAudioVariant`. The screen `PosterCustomizeScreen` (app:settings) forces SENSOR_LANDSCAPE (DisposableEffect restores the previous orientation), left panel ~32% (elements + visibility + 12-swatch palette + style reset + the bottom size slider), right = the 2.56:1 editable preview (one keyless `pointerInput(Unit)` — a state key would restart the detector mid-drag; awaitEachGesture: 1 finger drag, 2 fingers pinch, topmost-first hit-test; magnetic snap = 8px grid + 14px magnetism to margins/centers/other elements' center lines with lime guides; the pinch→drag handoff re-anchors prev). FLOW SEEDING: the first edit seeds untouched elements at their flow anchors and flips `customized=true`; the composer renders ABSOLUTE mode from the saved JSON (the v1.1.14 prefs still gate audio badge/episode title/thumbnail). SAVE: persist + `Logger.i("Anikuta:App:PosterStudio") { "POSTER LAYOUT SAVED: <full json>" }` — THE USER READS THIS LOG TO HAND US THEIR LAYOUT AS THE FUTURE DEFAULT. Reset restores the factory flow layout.
- **The shuffle feedback (D-506)**: preview pulse + icon 360° spin + banner Crossfade on every shuffle tap; D-494 semantics untouched.
- **v1.1.15 is the next release** (D-498 standing process): cut release/1.1.15 from the green feature head as ONE commit (1.1.15/10115 + the 44-RELEASE record) the moment CI is green; the user's device round happens ON the release APK.
- **v1.1.15 is LIVE** (D-507): release/1.1.15 = feature head `048f9422` + ONE bump commit (5974113a, 1.1.15/10115 + the 44-RELEASE record); implementation run 35465964501 GREEN (after the one-fix-push for the studio's wrong composer import), Release APK run 35466505113 GREEN on the FIRST run; the user's device round happens ON this release — the checklist in progress.md (chips both / thumbs / full-brightness / banner-only notification / real heads-up + 30s / the studio / shuffle animation / offline).

## 10. ROUND 53 (D-508..D-517) — the studio obeys the finger; the notification speaks again
The v1.1.15 device round approved the studio and asked for nine refinements. What the next agent must know:
- **THE DRAG MODEL IS ABSOLUTE (D-508)** — never go back to per-event delta accumulation through `origin()`: the D-503 loop wrote the SNAPPED position back every frame, so any sub-14px movement was swallowed forever (the user's "slow swipe never escapes the magnet"). `moveElementTo(kind, rawX, rawY)` receives the raw target computed from the SEGMENT anchor (the element's origin + the pointer position captured ONCE at gesture down / pinch start / pinch→drag handoff); the snapped value never feeds the accumulation. The studio's clamps MIRROR the composer's (texts x ≤ W−120, y ≤ H−lineHeight×scale; chips/cards measured) — keep them in lockstep with composeAbsolute or WYSIWYG dies.
- **The rich style layer (D-511)**: `PosterElementLayout` now carries fontKey/bold(Boolean?)/italic/shadow/chipBgArgb/labelOverride — ALL with factory-look defaults (old saved JSON parses untouched; `bold=null` = the element's factory weight). Rendering goes through `PosterDrawing.typefaceFor(fontKey, bold, italic, fallbackBoldDefault)` — ONE resolver for composer AND studio. The audio row's comma-split override lives in the composer's `audioChipLabels` and the studio's `chipLabelsFor`.
- **The studio is a DESIGN CANVAS (D-513)**: BOTH tags render regardless of availability (the real banner stays truthful via the resolved variant); the canvas opens on the settings screen's EXACT selection (`PosterCustomizeKey` = @Serializable data class carrying mainId/title/episodeNumber/variant; `onOpenCustomize: (PreviewSelection?) -> Unit`). `PreviewSelection` is PUBLIC (an internal type in a public composable's parameter does not compile — the round's one CI error).
- **The scrim is BACK, SMARTER (D-514)**: `PosterDrawing.applyAdaptiveScrim` measures the background's 24×10 luminance and veils proportionally (0% ≤0.40 → 46% at 1.00) — called on the composer AND the studio right after the cover-crop. The D-504 "no darkening" rule is SUPERSEDED; the D-499 shadows stay (now a per-element toggle).
- **The notification speaks (D-516)**: when a banner composes, the builder title = `headline(title)` (40 chars + "…") and the text = `shortDescription(title)` ("New episode available for " + the first two words) on BOTH post paths; the BigPicture style stays clean (no bigContentTitle/summaryText). The D-503 empty-title rule is SUPERSEDED. Android cannot force-expand a heads-up — title+text is the honest collapsed-state fix.
- **wrappedLines hard-ellipsizes EVERY line (D-512)** — a single long word no longer overflows its column; the studio's elementRect uses the SAME wrap math (its width floor is 24px, mirroring the composer's floorless W−x−margin bounded by the x ≤ W−120 clamp).
- **v1.1.16 is LIVE** (D-517): release/1.1.16 = feature head `3cb5adcc` + ONE bump commit (b87f7fd0, 1.1.16/10116 + the 45-RELEASE record); implementation run 35470221593 GREEN (after the one-fix-push for the PreviewSelection visibility), Release APK run 35470742809 GREEN on the FIRST run; the user's device round happens ON this release — the 9-point checklist in progress.md (slow drag / slider below / real buttons / rich options / long titles / same content + both tags / smart darkening / title + description on the heads-up / old layout still loads).

## 11. ROUND 54 (D-518..D-521) — the studio gets a shell, the title gets aware, the shuffle always shuffles
- **The shell (D-518):** the round-54 device round found the studio's header UNDER the status bar (zero insets on a forced-landscape edge-to-edge screen) with an unclickable Save. Root Column → statusBarsPadding; full-width header (back · title · labelled Reset · labelled primary Save); body Row carries navigationBarsPadding (either landscape edge). Left panel = tonal SectionCards; right pane = the untouched gesture canvas + a carded slider strip. DSL trap recorded: LazyListScope `items{}` cannot run inside a plain composable lambda — the ELEMENTS card uses a plain `for`.
- **The aware columns (D-519):** `PosterDrawing.awareWrapWidth` — a text element's wrap width ends before the left edge of any visible neighbour sharing its band. Band is TWO-PASS (lines counted at full width first — one-line titles are never shrunk by chips below them). Neighbour rects carry left/top/bottom ONLY (no measurement → composer/studio identical). AWARE_MIN_WIDTH=120 floor; candidates below it are skipped, never collapse the column. The composer clamps the awareness anchors exactly as it renders (legacy JSON safety); the studio declares awarenessRects/awareWidthFor BEFORE elementRect (Kotlin local funs are not visible above their declaration — the round's one CI-validated blocker) and passes the hoisted per-recomposition widths into drawPoster. THE CRASH FIX: the composer's thumbnail render clamp was the last unguarded coerceIn — saved thumb scale > ~1.78 (the studio's slider allows 2.0) threw IllegalArgumentException → every real notification silently fell back to text. All clamps now range-guarded.
- **The shuffle (D-520):** the D-494 exclude handshake no-op'd when the on-stage id was still null (a tap before the first compose finished reused the cached selection — pulse with no change). Now an explicit shufflePending flag consumed first on Main per pass (mid-flight taps cancel + the next pass re-reads), "" sentinel keeps the deck path alive with nothing on stage, and a `shuffling` state (finally-cleared, cancellation-safe) drives an indeterminate rail on the preview's bottom edge while the new banner composes.
- **The text (D-521):** shortDescription() = "New Episode" on both banner paths (headline keeps the content name).
- Released as v1.1.17 per D-498 (the 46-RELEASE record lives on the release branch). Full round record: memory/progress.md Round 54; decisions D-518..D-521.

## 12. ROUND 55 (D-523..D-526) — THE STUDIO IS RETIRED; THE FIVE TEMPLATES ARE THE LAYOUT NOW
The v1.1.17 device round reversed course HARD: "we are doing a little bit overboard ... We should not give the users that much customizability ... remove the poster studio functionality completely. Instead of that we should provide the user with some predefined templates." What the next agent must know:
- **THE STUDIO IS GONE — do not resurrect it**: PosterCustomizeScreen.kt, PosterCustomizeKey, onOpenCustomize, PosterLayoutConfig.kt, posterLayoutJson and the composer's ABSOLUTE mode (composeAbsolute/loadEditorArt/typefaceFor/awareWrapWidth) were ALL deleted. §9/§10/§11's studio knowledge above is HISTORICAL — the drag/pinch/magnet/snap/flow-seed/JSON-save machinery no longer exists anywhere in the codebase.
- **PosterCanvasMetrics now lives in PosterTemplate.kt** (it used to live in the deleted PosterLayoutConfig.kt — the review caught the dangling object). The 1024×400 geometry constants are unchanged.
- **THE FIVE TEMPLATES (D-524)**: `PosterTemplate` (classic/spotlight/split/minimal/card) → one private compose* renderer in EpisodeBannerComposer each, all deterministic flows on the shared primitives (drawTagRow/tagRowWidth/audioChips/drawCenteredText/drawBranding(topAligned) are the extracted helpers). The choice is the `notif_poster_template` string pref (NotificationPreferences.posterTemplate, default "classic"; fromKey is lenient — unknown → CLASSIC). NO anchor JSON exists anymore; a legacy saved layout is simply ignored.
- **THE SCREEN SHAPE (D-525)**: NotificationPosterSettingsScreen = CollapsingHeader + a STATIONARY region (the live preview card + the full-width "Shuffle" button — OUTSIDE the list, never scrolls) + a LazyColumn (Layout 5-way → Artwork 3-way → Elements switches). The local PosterCard replaces SettingsGroupCard on this screen because the shared card bakes in 16dp horizontal padding — the screen wants ONE 8dp gutter (the user's padding complaint). The shuffle semantics (D-494/D-499/D-520) are untouched; the produceState keys include templateKey + backgroundSource.
- **THE ARTWORK SOURCE (D-526)**: posterBackgroundSource = "banner" (Auto) | "cover" | "episode" (NEW — the episode's still fills the STAGE first; the ungated load only runs when the card gate made the first chain walk a no-op). "Prefer cover art" switch removed.
- **SegmentedToggle** (app/settings) gained `compact: Boolean = false` (12sp labels) — the five-way picker needs it; existing call sites are unaffected (default param).
- **v1.1.18 is the release** (D-498 standing process): cut release/1.1.18 from the green feature head as ONE commit (1.1.18/10118 + the 47-RELEASE record) the moment CI is green; the user's device round happens ON the release APK — the 9-point checklist in progress.md.

## 13. ROUND 56 (D-528..D-534) — the templates round two: Classic reimagined, Split became Duo, the page collapses to the toggle
The v1.1.18 device round was satisfied with the template SYSTEM ("handled exactly how I hoped") and picked three winners (Spotlight "quite good", Minimal "satisfied but improvable", Card "the best one") while demanding three rebuilds and three cross-cutting cleanups. What the next agent must know:
- **CLASSIC v2 (D-528) is NOT the old classic**: the text stack is now vertically CENTERED (a measured stack, Spotlight-style) with the tag row as an EYEBROW on top of the title; the still card is 360×202.5 (CLASSIC_THUMB_W/H in the companion). drawFlowThumbBox is gone.
- **DUO replaced SPLIT (D-529)**: enum entry DUO("duo","Duo"); composeDuo draws the left half full-bleed (corner 0), a solid right panel (DUO_PANEL_COLOR val, NOT const — .toInt() isn't a constant expression), and a 5px lime seam; fromKey ALIASES "split"→DUO and the settings screen seeds templateKey through fromKey (the reviewer's RISK-1 — a raw key misses the enum and lights the wrong segment). The composer's textX in Duo is panel-relative; branding sits ON the panel.
- **THE COLLAPSE (D-531)**: both the stationary preview region and the scrollable options region wrap in AnimatedVisibility(posterEnabled); the master switch lives in its own UNLABELED PosterCard (the label param is String?=null now) at the very top of the stationary region; produceState early-returns when off. If you add options, put them INSIDE the collapsed regions.
- **THE ONE-LINE LANGUAGE (D-532) is STRUCTURAL now**: the shared SettingRow description and MoreListRow subtitle (2→1) clamp to one line — new rows anywhere inherit it. Keep copy ≤ ~40 chars; the clamp is the guarantee, not the design. The notifications page's four cards pass showDividers=false (no trailing lines).
- **BadgeIcons.Total is a PLAYLIST (D-533)** — and the WHY matters: Compose Icon tint flattens ALL path fills into one color, so any two-color icon design silently degrades to its silhouette. Design every badge icon as a one-color silhouette.
- **Browse speaks the D-480 soft-pill language (D-534)**: PointedTagShape has ZERO remaining users (the file stays as API for now); the hero score is its own amber pill via rememberBadgeColorScheme.
- **v1.1.19 is the release** (D-498 standing process); the user's device round happens ON the release APK.

## §14 — Round 57 (the device round on v1.1.19): the toggle's FINAL home + the CloudStream downloads round

**Poster verdicts (do not touch):** ALL FIVE templates are proper now — Classic v2, Spotlight, Duo, Minimal v2, Card ("the poster templates are quite satisfactory and according to how I wanted them to be"). Browse + Library: "quite satisfied".

- **THE TOGGLE'S FINAL HOME (D-536)**: the master switch is the LAST row of the ELEMENTS card, titled **"Poster"** — NOT its own top card (D-531's placement lasted exactly one round). When off, the preview, Layout, Artwork, the Elements label and the four element rows EACH collapse (per-region AnimatedVisibility — the whole-list wrapper is gone because the toggle lives inside the list); the lone "Poster" row survives. If you add poster options, put them INSIDE the collapsed regions as before — the toggle row itself must stay last and always visible.
- **THE CS PLAYER PAUSES ON BACKGROUND (D-537)**: the CS watch screen now carries the MPV watch screen's exact LifecycleEventObserver (ON_STOP → engine.pause(), ON_START → no auto-resume — the user taps play on both players). Keep any new player screen at this parity.

**The CloudStream download system (D-539) — the durable knowledge:**
- A DASH download is NOT a file: manifest + segments in SimpleCache(filesDir/cs_dash_cache, NoOpCacheEvictor), keys `csdash|<manifestUrl>|<resourceUrl>` (DashCacheKeys in core/common — the shared contract; the downloader writes, the player reads, delete purges).
- The SAF folder still gets .data.json (+ dashManifestUrl per episode entry), .cover.jpg, .nomedia, subtitles/ — the scanner registers cache episodes WITHOUT a file walk; DataJsonRepair MUST propagate dashManifestUrl on rebuilds (losing it = anti-shrink churn + unrecoverable after reinstall).
- downloaded_episode rows: video_uri = `csdash:<manifestUrl>` (the marker every router switches on), file_size = real cached bytes — NO schema change (no migration chain exists; a .sq constraint change would crash updated installs).
- Offline playback: CsWatchKey(offlineManifestUrl=...) → CsWatchViewModel.startOfflinePlayback → engine.startOfflineDash (DashMediaSource over CacheDataSource + the shared key factory + maxVideoHeight pin; startInternal clears the pin). Push the key DIRECTLY to the backstack — csResolveRequest opens the resolve sheet (the review blocker).
- The resolve sheet's DASH filter is GONE — DASH links download; DRM manifests fail honestly in the queue ("This stream is DRM-protected — it cannot be downloaded").
- The upstream client for segment fetches = the CS runtime's base client (named("cloudstreamPlayback")) — the resolve just succeeded, so its cookies are live.

**The data translation (D-540):** CS-bridged ids carry bit 62 (core/content's CLOUDSTREAM_SOURCE_ID_FLAG — the documented twin of data:cloudstream's CsSourceIds). New CS content records system/extension_type "cloudstream"; healCloudstreamEcosystem (AnikutaApp startup, BEFORE the download scan) relabels old rows idempotently. ContentIdentitySync fires on every contentId regeneration: the two previously-dead .sq queries (updateDownloadedContentId / updateDownloadContentId) sync the download tables + the .data.json identity block rewrites through the verified ladder. Downloads SURVIVE source switches (files are mainId-keyed); episode keys stay source-specific by design.

**The auto-link skip (D-538):** persisted per (sourceId, animeUrl) — `auto_link_user_skipped:` prefix; attemptAutoLink checks it BEFORE the cache check; explicit sheet skip persists, swipe-dismiss closes only; manual link AND unlink clear it. If you touch the manual-link sheet: the Skip button = skipAniListLink (persists), dismiss = dismissManualLinkSheet (does not).

## §15 — Round 58 (the device round on v1.1.20): the DASH fetch fix + the branch cleanup

**The verdicts:** the poster toggle (Elements' last "Poster" row), the CS background pause, and the auto-link skip persistence ALL passed the device round. The one defect: the DASH download queued, fetched the manifest clean (200, 4012 bytes), then "Manifest could not be parsed" (DashDownloader.kt:87). Plus the user's housekeeping ask: prune the stale branches.

**The fetch fix (D-543) — the durable knowledge:**
- The root cause was the manifest reaching the DOM parser as a decoded-and-re-encoded STRING (`body.string()` → `byteInputStream()`): forced UTF-8 on `application/octet-stream` bodies (a UTF-16 MPD = NUL-riddled mojibake), gzip binary undecoded (OkHttp transparently gunzips only when IT supplied Accept-Encoding — the surviving Content-Length in the logcat proves none happened). Streaming worked because media3 parses RAW bytes.
- THE RULE: the manifest travels as BYTES end to end. `DashDownloader.fetchManifestBytes` (body.bytes(), empty/8 MB guards, gzip via Content-Encoding header OR the 1F 8B magic, bounded decompression) → `DashManifestPlanner.parse(ByteArray, …)` (platform BOM/UTF-16 detection; parse failures carry the REAL parser message + a sanitized ≤80-char body-head peek — never re-introduce a catch-all that discards the cause).
- The retry contract: manifest transport errors = raw IOExceptions (RetryPolicy retries them + the queue's pauseForNetwork gate applies); non-2xx = HttpException (5xx/429 retry, 4xx fail fast); gzip/parse/oversize failures = DownloadException (honest ERROR). Wrapping an IOException into a DownloadException re-introduces the round-58 bug.
- Full record + the failure matrix: download-research/19; doc 18 §7 is the addendum. The `findContentFolder — NO folder` WARN in download logs is BENIGN (the notification's cover-thumbnail lookup; the folder exists only after publish).

**The branch cleanup (D-544):** the remote branch set is main + feature/round-57-cloudstream-downloads + feature/test-controller-v5 — everything else deleted AFTER per-branch verification (release/1.1.x = tag-preserved, docs-only deltas; old feature tails = docs-only LIVE-line records). Releases live on TAGS: deleting a release branch deletes nothing a release needs. Verify with `git diff --name-only <tag> origin/<branch>` before any future prune.

**Round-58 state:** v1.1.20 confirmed good except the DASH download path; the fix rides release/1.1.21. Main stays 0.4.20/85; merge to main still awaits the user's explicit confirmation.

## §16 — Round 59 (the device round on v1.1.21): the parser factory fix — the REAL root cause

**The verdict:** the DASH download still failed on v1.1.21 with "Manifest could not be parsed: This parser does not support specification "Unknown" version "0.0"" — over a body the new D-543 diagnostics PROVED to be valid XML. That combination (valid body + spec-version error) means the parser's CONSTRUCTION, not the parse.

**The root cause (proven against AOSP libcore, not theorized):** Android's `javax.xml.parsers.DocumentBuilderFactory` base class throws `UnsupportedOperationException` — verbatim "This parser does not support specification "Unknown" version "0.0"" — from `setXIncludeAware`, `isXIncludeAware`, `setSchema`, `getSchema`, UNCONDITIONALLY, even for `setXIncludeAware(false)`. Both hardened factories called `isXIncludeAware = false` unwrapped inside the `.apply{}` → the factory exploded before one byte of XML was read, on EVERY device, since the pipeline existed. v1.1.20 and v1.1.21 failed at the SAME line; D-543's byte-first contract was real but secondary (and its diagnostics are what exposed this).

**THE RULE (durable):** on Android, NEVER call `setXIncludeAware`/`setSchema`/`getSchema`/`isXIncludeAware` on `DocumentBuilderFactory` — any of them throws, always. Safe hardening = the wrapped `setFeature` URIs + `isExpandEntityReferences`. Also: parse-failure messages must ALWAYS carry the exception class (`simpleName: message`) — message-only strings cost a device round-trip per bug. And a derived `client.newBuilder()` patient client (shared pool/interceptors — cookies/clearance still ride) is the pattern for one-off cold-start-sensitive fetches; don't raise timeouts globally.

**The fix (D-546):** the throwing line removed from BOTH factories (`DashManifestPlanner` + `MpdParser` — the latter had been silently swallowing the crash, so its on-device "DASH hidden" verdicts were never real); parse errors carry the class name; the manifest fetch gets connect/read 20s + call 45s; segments/subtitles keep the shared client. Blast radius: 3 files, zero behavioral overlap with streaming/queue/storage.

**Round-59 state:** fix rides release/1.1.22. The branch set (main + feature/round-57-cloudstream-downloads + feature/test-controller-v5 + the live release/1.1.21) is the D-544 shape — nothing further to prune. Main stays 0.4.20/85; merge to main still awaits the user's explicit confirmation.

## §17 — Round 60 (the device round on v1.1.22): D-548 — the DASH media home moves into the SAF folder

The v1.1.22 round validated the download pipeline (downloads complete, play, show on the downloads page, .data.json updates properly) and rejected one thing: the video file was not in the user's selected download folder (D-539 had put the media in the app-private SimpleCache). D-548 re-homes it: real files (`<title> - E00001.mp4` + `<title> - E00001.audioN.mp4` + `<title> - E00001.mp4.dashmeta`) under `video/<title>/episodes/`, playback via the new sideloaded-manifest `startOfflineDashLocal` + `LocalDashDataSource` (an unindexed fMP4 is unseekable in ExoPlayer — the manifest IS the index), the `csdash:` marker payload decodes by mode (legacy manifest URL vs local sidecar uri), pause/retry resume rides a manifest-hash-pinned sidecar, the scanner/delete/token-matching all understand the audio+meta siblings, and the SimpleCache remains only for the v1.1.20–v1.1.22 legacy episodes. The independent review's both blockers (SAF file renaming via mime/ext reconciliation; the scanner rebuild dropping dashManifestUrl) + one major (DataSpec.position = absolute range start) were fixed before push. Record: download-research/21.

## §18 — Round 61/62 (the device round on v1.1.23): D-550 — the sidecar manifest is COMPOSED (the ABR fix), audio versions switch OFFLINE, the audio/ folder

The v1.1.23 round validated the real-files layout but hit two things: the downloaded episode WOULD NOT PLAY, and the user asked for a plan for the three files (video / audio / .dashmeta) — including offline audio-version switching and a dedicated `audio/` folder. ROOT CAUSE of the playback failure: the sidecar stored the ORIGINAL manifest, whose video set lists THREE representations (1080p/720p/480p) while only the picked one is on disk — ExoPlayer's ABR boots on a 1 Mbps initial estimate, picked the 720p rep, and died on the first local-index miss (the height pin cannot prevent picking SMALLER reps). THE FIX, both ends: (1) READ side — `startOfflineDashLocal` prunes the sideloaded manifest to the reps the range index covers (`DashManifestPruner.coveredRepIds`/`pruneRepresentations`, new in `:core:common`, the planner's exact derivation mirrored; parse failure = untouched fallback) — the v1.1.23 episodes already on disk play WITHOUT re-downloading; the height pin is dropped for local-file episodes (stale-label risk, redundant post-prune) and kept for the legacy cache path; (2) WRITE side — `DashOfflineManifestComposer` composes the sidecar manifest: pruned to the downloaded reps + the siblings' audio AdaptationSets imported with ABSOLUTIZED addressing + every audio set LABELED (`<Label>` → Format.label). OFFLINE AUDIO SWITCHING: the enqueue step collects the sibling DASH audio-variant manifests (same server, different audio label) as `TrackKind.AUDIO_VARIANT` tracks through the existing audio_tracks JSON column (no schema migration; no other downloader reads them); DashDownloader plans + downloads their audio sets as extra groups (resume sha pins all manifests; failures best-effort) and the player's existing audio selector offers them offline. THE AUDIO/ FOLDER: audio sets publish to `<content>/audio/<base>.audio<N>.mp4` (like episodes/ + subtitles/); re-publish sweeps the v1.1.23 legacy-location copies; the delete sweep + scanner sibling accounting learn BOTH locations; data.json gains additive `audioUris` (ignoreUnknownKeys-safe; scanner rebuilds from disk). data.json otherwise untouched per the user. Record: download-research/22.

## §19 — Round 63 (the device round on v1.1.24): D-551 — one server, every audio version, every resolution

The v1.1.24 round CONFIRMED the whole D-548/D-550 chain on-device ("quite satisfied with the overall results") — offline playback, the audio/ folder, data.json all locked. Two new demands from the MovieBox dual-audio episode (Hindi + Original, one DASH manifest per variant): (1) formatted mode rendered the two audio versions as TWO separate servers ("MovieBox (Hindi Audio)" / "MovieBox (Original Audio)") instead of ONE server with two audio versions inside; (2) only ONE audio version downloaded — D-550's sibling pipeline never engaged. ROOT CAUSE (one, shared): the app's link-name vocabulary didn't understand LANGUAGE-AUDIO decorations — `CsAudioTag.parse` returned "Default" for both links, `serverNameOf` kept the bracket in the server name, and `siblingAudioVariants` matched siblings by RAW name equality (the two names differ → no siblings → no offline audio switch). THE FIX: (1) `CsAudioTag` gains the language-audio pass (bracketed + whole-segment forms, ~35-entry abbreviation map, multi-audio/sub/dub families excluded, no free-form matching — regexes PUBLIC as the single source of truth); (2) `serverNameOf` MOVED to `CsServerNames.of` in :core:cs-player because a third consumer needs it — the :app sibling matcher now derives the SERVER ("MovieBox") the same way the sheets group with it, repairing both symptoms with one vocabulary; (3) RESOLUTIONS (the user's conditional wish): `DashManifestHeights` (:core:common, pure, the pruner's hardened-factory discipline) + `CsDashQualityProbe` (impl-private; the LINK'S OWN headers — the MovieBox manifest needs its CloudFront cookie; bounded 5s/8s/2MiB; silent by contract) + additive `CsVideoLink.availableQualities` → the formatted server card renders `Available: 1080p · 720p · 480p` under a version whose single DASH link was probed (the same list the player's per-stream quality section shows); the probe runs off the resolve path (SupervisorJob scope dying with the sheet), the resolver flow stays byte-identical, and the enriched list rides `PreResolvedSeed` so the in-player sheet inherits it. Deliberately unchanged: RAW mode, the player, the downloaders, the queue, the scanner, the publish layout, data.json's schema, COMBINED sub/dub tags. Record: download-research/23.

## §20 — Round 64 (the device round on v1.1.25): D-552 — the resolutions become PICK TARGETS + the audio snippet fix

The v1.1.25 round CONFIRMED the D-551 grouping on-device ("it did show me everything as one single section and it showed me the two audio versions") but surfaced three demands: (1) the probed "Available: 1080p · 720p · 480p" line was DISPLAY-ONLY — "I was not able to click on them. I was not able to directly download them"; (2) the declared quality lied — the provider stamps each variant's manifest with its MAX rep, so "1080p = Hindi, 720p = Original" presented resolution as an audio-version property ("the actual thing was that 1080p resolution was available for the original audio too"); (3) BOTH audio versions downloaded this time (the D-551 sibling matcher engaged) but "audio 1 was not a proper one. It was just a random small snippet". THE TARGET: "exactly like how it would for the other extensions … I can click on any of the resolutions. It will directly start to play that resolution or directly start to download that resolution with the appropriate audio version alongside it", with the default shape unchanged ("both audio versions will be downloaded with the resolution of the main one"). THE SNIPPET ROOT CAUSE (code + elimination): `DashManifestPlanner.planFromTemplate`'s timeline branch expands every `r="-1"` SegmentTimeline entry to ONE segment when the MPD carries no parseable `mediaPresentationDuration`/`Period@duration` (`periodTicks = -1 → r = 0`) — audio packagers emit the whole track as a single negative-r entry, so the audio plan became init + 1 segment ≈ a few seconds, while the explicit video timeline stayed complete; and NOTHING downstream validated (the timeline-LESS branch fails loudly in the same situation — the timeline branch was the only silent one). THE FIXES: (A) THE PLANNER borrows the video timeline's span as the period clock — two passes per Period (video first, `spanSec` per rep; audio second with `periodDurationSec = declared ?: videoSpanSec`) — so audio r="-1" expands to the whole episode; `DashSegmentPlan` gains `videoSpanSec`/`audioGroupSpansSec` (single-period only — multi-period stays unvalidated against whole-manifest spans) + `audioSetsPresent` (declared-but-unplannable audio = a silent video), and the estimate's clock falls back to the derived span (real progress hints). (B) THE DOWNLOADER validates: primary audio known-short (<50% video span) → hard honest `DownloadException` (retryable, never a published snippet); sibling variants known-short → skipped (best-effort, the picked variant never pays); declared-audio-but-zero-planned-groups → refused; spans logged as evidence. (C) THE SHEET renders ONE CLICKABLE `CsQualityChip` PER PROBED HEIGHT for a single-DASH probed version — replacing the declared chip AND the "Available:" text; pick target = (link, height). (D) PLAY: `CsPlayerEngine.start/switchLink` gain `initialVideoHeight` → a one-shot `pendingVideoHeight` applied at READY via `selectVideoTrack` (the SAME TrackSelectionOverride the player's quality section uses — a selector, not the round-62 cap); not offered → cleared, ABR continues; reset/offline loaders clear it. (E) DOWNLOAD: the height rides `DownloadRequest.videoQuality` ("720p") — the pipeline's EXISTING carrier (queue label, DB column, data.json `quality`, `preferredHeightOf` → the planner's closest-rep pick); zero schema change; siblings stay resolution-independent. VERIFICATION MILESTONE: the planner is pure JVM, so the sandbox compiled + EXECUTED the tests for the first time (kotlinc + junit) — `DashManifestPlannerTest` (NEW, 8 locks incl. the snippet lock) 8/8 green executed, and the D-551 `DashManifestHeightsTest` was revealed to have NEVER compiled (`$Number%05d$` is a Kotlin template = compile error; CI runs assembleDebug only, test sources never compile) plus a prolog-indent fixture bug — both repaired, 6/6 green executed. In passing: the timeline-less branch's `duration * timescale` double-scaling was fixed to the spec reading (@duration is already in ticks) — it would otherwise have masked a snippet behind a huge span post-cross-derivation. Untouched: RAW mode, sub/dub grouping, the probe, the publish layout, data.json's schema, the queue/scanner, the streaming player's own quality section. Record: download-research/24.

## §21 — Round 65 (the device round on v1.1.26): D-553 — the selection TRUTH (the height memory + the honest markers + the reachable quality section)

The v1.1.26 round ran as a LOG-CAPTURE round first (no code, no push): the deliverables were the Android-Studio logcat filter (`tag:Anikuta` — every app/vendored-CS tag starts with the prefix) and a detailed AI-agent prompt that formats the raw logcat into a phased digest (resolve → probes → pick chains → in-player switching → anomalies). The user's capture (Moon Knight EP 2, MovieBox, 4 audio versions Hindi/Original/Malayalam/Tamil all probed 1080·720·480) PROVED the D-552 chain exact — `picked: … (Malayalam) at 480p` → `play request: … startAt=480p` → `READY: track groups video=1 audio=1 text=0` → `start-height pin: 480p → video group=0 track=2` → `video track selected: 480p (g0/t2)`, decoder confirmed 856x480. **480p genuinely played** — yet the user "had concerns that maybe it was playing in 1080p". That sentence was the diagnosis: the truth was UNOBSERVABLE behind three defects. (1) THE RE-ENTRY HOLE: the same-key re-entry + autoStart re-requested play with NO initialHeight (only the SEEDED path pinned) — the link was remembered per show but the height was not, so re-opening an episode or auto-advancing silently fell back to ABR → 1080p over the user's pick. (2) THE LYING MARKER: selectedTrackLabel was computed from group.isSelected (true for the WHOLE group when ANY track plays → firstOrNull always returned the FIRST rep = the TOP quality — the marker would have highlighted 1080p while 480p played). (3) THE UNREACHABLE SECTION: CsLinksSheet was a non-scrolling Column; one tall MovieBox card (1 server × 4 versions × 3 chips) pushed "Quality for this stream" out of the clipped bottom — composed but INVISIBLE. THE FIXES (all in v1.1.27): CsSourceMemory's height half (written by CsResolveSheet.pick + selectLink — a null-height pick CLEARS, a declared chip cannot pin so it must not fake a preference; read by re-entry + autoStart when the remembered server matched; a stream lacking the height self-heals via "not offered → ABR"); isTrackSelected(trackIndex) + engineState.videoHeight in the open-effect keys (live refresh); the accordion's exactly-ONE lit chip (current link × the LIVE decoder height) + the version-label tint + the "Now playing: <audio> · <label>" line; the one-body verticalScroll sheet (accordion/raw/variants LazyColumn→Column — a LazyColumn under verticalScroll would measure with infinite height constraints and crash; the resolve sheet got the same parity scroll); shortAudioLabel for the collapsed header at 3+ versions (HIN/ORIG/MAL/TAM + curated map + 3-letter fallback, SUB/DUB pass through; ≤2 keep full names; 6 unit tests sandbox-executed green pre-push — the bare-"Audio" expectation was corrected BY that execution, the 3-letter fallback is the honest output); and the decoder truth log (onVideoSizeChanged → "video size: WxH — playing <label>", deduped). RELEASE LEDGER: implementation run 35625463149 GREEN first-try; release run 35626504172 RED — the workflow titles the GitHub Release from the TAG ANNOTATION'S SUBJECT and the tag carried the full commit message (HTTP 422 name >256 chars AFTER every gate passed); the re-cut concise tag → run 35627061143 GREEN; 3 runs vs the ≤2 budget, 1 RED, disclosed. Untouched: RAW mode contents, sub/dub grouping, the probe, the download pipeline, data.json's schema, the queue/scanner, the subs/episodes sheets. Record: download-research/25.
