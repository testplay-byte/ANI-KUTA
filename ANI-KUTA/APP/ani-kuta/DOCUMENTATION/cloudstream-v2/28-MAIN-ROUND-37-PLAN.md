# Round 37 — Task 77: the MAIN-branch convergence round (the release-line features ported to main, the app-icon system rebuilt catalog-only, the repo cleanup, and the v1.1.1 re-release with the icon fixes)

**Branch:** `main` (the dev line — the debug bubble STAYS here; the publishable `release/1.1.1` line keeps its own clean state)
**Version:** 0.4.20 / 85 on main (UNCHANGED — version discipline, D-425: the version never moves without the user's explicit instruction); the release line stays 1.1.1 / 10101.

## The user's round (verbatim intent)

1. **Work on main again** — "we are going to work on the main branch again and apply some changes to it… implement some of the features which we implemented in the release branch to the main branch because we added these features later on."
2. **Release discipline stands** — "we are going to properly build releases from now on. Make sure to handle that properly when we are actually doing a release; then all those things need to be followed. Besides that those things will not be followed on the main branch." → the full release routine (all-ABI + ZIP + GitHub release) runs ONLY on actual releases (the tag-driven path); main pushes build debug + release APKs for testing.
3. **The debug bubble STAYS on main** (it was removed from the release line in D-409 — that stays removed THERE; main keeps it).
4. **The episode-check empty notification fix** (D-410) → port to main.
5. **Remove the unused dependencies and directories** from main (D-414's dependency cleanup + the provably-unused repo-root clutter).
6. **The share button fully hardened** (D-415) → port to main.
7. **The app icon system, rebuilt properly:**
   - the 8 premade baked app icons REMOVED completely ("There are 8 premade app icons on the app icon page but I told you to remove all of them and you did not. Remove them properly.") — no activity-aliases, no variant mipmaps, no baked previews; the App Icon page shows ONLY the GitHub repository's icons/ catalog;
   - the display format fixed: "I told you to show them in a circular kind of format but you are showing the app icons in a circle, which is not good." — **interpretation (documented honestly):** the icons are currently hard-cropped INTO circles (circle-clipped cells + pre-masked circle previews — the artwork's corners are cut, the same cropping the user rejected on the launcher in round 35). The new format shows each icon's FULL artwork in a ROUNDED-corner (squircle-style) cell — a "circular kind of format" that is NOT a circle crop. The selection ring matches the rounded shape.
8. **The About Minimize Debug Descriptions** (D-419, the exact three strings) → port to main.
9. **Repository cleanup:** delete `test-feature/video-cache-new-download`, `streaming/CLOUDSTREAM`, `streaming/CLOUDSTREAM-V2`, `functionality/improvements`; KEEP `feature/test-controller-v5`, `main`, `release/1.1.1`. (Done — 4 × HTTP 204.)
10. **The release branch + re-release:** "About the releases branch we are going to make a few changes in it and create a new release with the same exact version number and everything like that. Those changes are the app icon changes." → the same icon fixes on `release/1.1.1`, then the v1.1.1 tag + release deleted and RE-CREATED at the same version (1.1.1 / 10101) via the tag-driven CI pipeline.
11. **Build debug + release properly, kept separate, co-installable** (the D-416 line ported to main): debug `applicationIdSuffix ".debug"` + `versionNameSuffix "-debug"` + the old lime icon + "ANI-KUTA Debug" label → the user tests the official release + a main-branch debug build side by side.

## The key decisions

- **D-426 (the D-410 port):** `UpdateProgressNotifierImpl.onFinish` early-returns when `summary.totalChecked == 0` — the "nothing was due" notification is gone on main too. Identical hunk to the release line (the files were identical).
- **D-427 (the D-419 port):** the three debug-page subtitles → "Show stream URLs" / "Add a copy button to stream URL" / "Check the history". The debug-bubble Developer-tools section is untouched (main keeps the bubble).
- **D-428 (the D-415 port):** `CloudstreamPluginDetailScreen.sharePluginFile` bulletproofed exactly as the release line: pre-flight source check (specific message), 4s hard icon-fetch bound, 15s hard total bound, the ContextWrapper→Activity unwrap (NEW_TASK only when no activity), CancellationException rethrow + its own timeout toast, per-cause error messages, the >1h stale-export sweep. The file is taken wholesale from the release line (the diff was purely D-415).
- **D-429 (the co-install line + the latent-break fix):** the D-416 port (debug `.debug` suffix + `-debug` version suffix + the debug res overlay with the OLD lime icon + "ANI-KUTA Debug" label) AND the fix for the latent `assembleRelease` break D-409 found: **`app/src/release/java/.../DebugInit.kt` imported `DebugBuildInfo` from the debug-only classpath** (`debugImplementation(project(":feature:debug-bubble"))`) — release never compiled. On main the bubble STAYS, so the release DebugInit is rewritten to register NOTHING (empty Koin modules; the bubble is debug-only in both behavior and classpath). This is what lets main's CI run `assembleRelease` at all.
- **D-430 (the build line):** the release-line build config ported to main MINUS the release-only polish: (a) the convention plugin flips release to R8 full mode + resource shrinking with the D-413 keep surface in `app/proguard-rules.pro`; (b) the `keystore.properties`-driven `anikutaRelease` signing config (absent → unsigned-but-building, exactly the release line); (c) the app-module ABI `splits` block (active only under `-PreleaseAllAbis=true` — the tag path; main pushes stay single-ABI arm64); (d) `build-apk.yml` gets the D-423 treatment — the secrets decode + SIGNED push-path assembleRelease + the apksigner gate + mapping.txt. NOT ported (release-only per the user's rule): D-411's updater repoint (main's updater keeps pointing at the dev repo), D-412's release logging strip (main keeps its dev logging).
- **D-431 (the unused-dependency + directory cleanup):** the D-414 port — logcat (14 module declarations), stray rxjava (3), seeker, androidx-media, truetype-parser (each re-verified zero-import on main before removal) + the 5 hardcoded deps moved into the catalog. Repo-root audit: `DATABASE.json` (the old debug-bubble DB export — analysis long since recorded in the docs) DELETED; the stray root `1788604676536.jpg` (the user's icon upload — now embedded in the app res) MOVED into `USER-UPLOADS/` (user data is preserved, the root is clean). KEPT deliberately: `USER-UPLOADS/` (the user's zone — the extension APKs were saved there "per user request"), `ANI-KUTA/REFERENCES/` (the old-vs-new knowledge base), `ANI-KUTA/DASHBOARD/` (actively deployed to GitHub Pages by deploy-dashboard.yml), `:feature:debug-bubble` + `:core:debug-api` (the bubble stays on main).
- **D-432 (the app-icon system, catalog-only):** the user's kawaii-mouth icon is main's launcher icon (the D-421-safe adaptive layers + legacy WebPs ported from the release line's V1 set; the old lime vectors move to the debug overlay). The App Icon page is REBUILT: hero + ONE grid of the GitHub `icons/` catalog (live fetch → offline cache → refresh), each icon shown as the FULL artwork in a rounded-corner cell (no circle crops anywhere), tap = in-app apply with the honest note, a clear-override row. `AppIconPreferences` loses the alias machinery (`activeAlias` is gone — there are no aliases); `catalogJson` + `inAppOverridePath` remain. NO custom-image import (removed in D-422, stays removed).
- **D-434 (the release-line icon rework + the v1.1.1 re-release):** on `release/1.1.1`: the 8 activity-aliases deleted (MAIN/LAUNCHER returns to MainActivity — the user's kawaii icon IS the launcher icon), all `_v2.._v8` mipmaps + `icon_preview_v*` + the orphaned `ic_launcher_bg_v1.webp` deleted, the same catalog-only App Icon page. Then the OLD v1.1.1 tag + release deleted and the tag re-pushed on the new commit (the SAME version 1.1.1 / 10101 — the user's explicit instruction) → release-apk.yml rebuilds: all four ABI APKs + universal + the ZIP + SHA256SUMS + mapping, all CI-signed. Honest note: same versionCode means a device holding the OLD v1.1.1 must uninstall once before installing the re-release.

## Verification plan

1. Main push → Build APK workflow GREEN (tests + assembleDebug + the SIGNED assembleRelease + the apksigner gate + ABI check) — this also proves the release DebugInit fix (release now compiles WITH the bubble modules still present).
2. `aapt`-level checks are CI's job (never build locally); the workflow's own ABI + signature gates are the verification.
3. Release-line push → Build APK GREEN; then the tag re-push → Release APK workflow GREEN → the new release assets downloaded + sha256 + apksigner + version checks (10101 / 1.1.1).
4. Docs: this plan + the memory records (progress / changelog / decisions D-426..D-434) + BUILD-GUIDE (the release-vs-debug division, how the user tests both side by side).
